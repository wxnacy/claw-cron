## Projects

版本号修改位置 `src/claw_cron/__about__.py:__version__`

## DONE

- [x] 修复 daemon 模式下环境变量丢失问题 ✅ 2026-05-06 15:10:00
    - 问题：`claw-cron run` 正常执行，`server` 模式下 `${OBSIDIAN_HOME}` 等 shell profile 变量缺失
    - 原因：daemon 双 fork 后未经过 login shell 初始化，profile 变量不在 `os.environ` 中
    - 方案：新增 `_load_shell_env()` 通过 `$SHELL -l -c env` 捕获 login shell 环境，带全局缓存
    - 修改：`src/claw_cron/executor.py`
    - 提交：c47ec1d, 438c5b4

## TODO

- [ ] 为 Task 增加 `cwd`（工作目录）支持
    - 需求：允许任务指定执行时的工作目录，使脚本中的相对路径（如 `./run.sh`、`python ./helper.py`）能正确解析
    - 方案：
      1. `src/claw_cron/storage.py` — Task dataclass 新增 `cwd: str | None = None` 字段
      2. `src/claw_cron/executor.py` — `subprocess.run()` 传入 `cwd=task.cwd`（command 和 agent 类型）
      3. `src/claw_cron/cmd/add.py` — `add` 命令新增 `--cwd` 选项
      4. `src/claw_cron/cmd/command.py` — `command` 命令新增 `--cwd` 选项
      5. `src/claw_cron/cmd/list.py` — list 表格新增 CWD 列
      6. `src/claw_cron/executor.py` — `temp_task` 构造时传递 `cwd=task.cwd`
    - 注意：
      - backward compatible，已有任务无 cwd 字段时默认为 None，行为与现在一致
      - `reminder` 类型不执行脚本，无需处理 cwd

- [ ] 版本号和文档一致
