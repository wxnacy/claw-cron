## Projects

版本号修改位置 `src/claw_cron/__about__.py:__version__`

## TODO

- [x] 版本号和文档一致 ✅ 2026-05-06 15:08:00
    - 功能说明：将 `src/claw_cron/__about__.py` 中的版本号从 `0.3.4` 修改为 `0.3.5`
    - 修改内容：
      - `src/claw_cron/__about__.py`：版本号更新
    - 提交记录：
      - a9ad37f chore(version): bump version to 0.3.5
- [x] 增加 `info [name]` 命令 ✅ 2026-05-06 15:20:00
    - 需求细节：
        - 竖向打印单个任务的详细信息
        - 跟 list 一样使用表格打印，第一列是字段，第二列是内容
    - 功能说明：
      - 新增 `info [name]` 命令，`name` 为可选参数
      - 不传 `name` 时，通过交互式 fuzzy 选择器让用户选择任务（支持上下键和输入过滤）
      - 使用 Rich Table 竖向展示任务字段：Name、Cron、Type、Enabled、Script/Prompt、Client、Client Cmd、CWD、Message、Channels、Env
    - 解决方案：
      - 新建 `src/claw_cron/cmd/info.py`，实现 `render_task_info()` 和 `info()` 命令
      - `cli.py` 注册 `info` 命令
      - `prompt.py` 新增 `prompt_fuzzy_select()` 工具函数，基于 InquirerPy 的 `fuzzy` 组件
    - 修改内容：
      - `src/claw_cron/cmd/info.py`：新增 info 命令模块
      - `src/claw_cron/cli.py`：注册 info 命令
      - `src/claw_cron/prompt.py`：新增 `prompt_fuzzy_select()` 函数
      - `AGENTS.md`：更新 Key Files 和 Commands 说明
      - `README.md`：更新任务管理使用说明
    - 提交记录：
      - fe62032 feat(info): add info command to show task details
      - 3bb811d docs: update AGENTS.md and README.md for info and delete enhancements
- [x] `delete` 功能增强 ✅ 2026-05-06 15:22:00
    - 需求细节：
        - `name` 改为可选参数，如果不传，交互式提供列表让用户选择，可以上下键选择，也可以输入信息过滤
        - 获取到 `name` 后，先调用 `info` 打印任务详情，在进行后续确认或直接删除操作
    - 功能说明：
      - `delete` 的 `name` 参数改为可选
      - 不传 `name` 时通过 `prompt_fuzzy_select()` 交互式选择任务
      - 确定任务后先调用 `render_task_info()` 打印详情，再询问确认（`-y` 可跳过）
    - 解决方案：
      - 修改 `src/claw_cron/cmd/delete.py`，`name` 参数设为 `required=False`
      - 引入 `render_task_info()` 和 `prompt_fuzzy_select()` 实现交互选择和详情展示
    - 修改内容：
      - `src/claw_cron/cmd/delete.py`：增强 delete 命令交互体验
      - `AGENTS.md`：更新 delete 命令说明
      - `README.md`：更新 delete 命令使用示例
    - 提交记录：
      - 8ab3646 feat(delete): enhance delete with interactive selection
      - 3bb811d docs: update AGENTS.md and README.md for info and delete enhancements
