---
wave: 3
plan: "03-04"
title: "log Command — Tail Task/System Logs"
depends_on:
  - "03-02"
files_modified:
  - src/claw_cron/cmd/log.py
autonomous: true
requirements_addressed:
  - EXEC-01
---

# Plan 03-04: log Command

## Objective

实现 `claw-cron log [name]` 命令，用 `tail -f` 语义实时跟踪任务日志或系统日志。

## Tasks

### Task 1: 创建 cmd/log.py

<read_first>
- `src/claw_cron/cmd/add.py` — Click 命令定义模式、Rich console 使用方式
- `src/claw_cron/executor.py` — LOGS_DIR 路径定义（`~/.config/claw-cron/logs`）
</read_first>

<action>
创建 `src/claw_cron/cmd/log.py`：

```python
# SPDX-FileCopyrightText: 2026-present wxnacy <371032668@qq.com>
#
# SPDX-License-Identifier: MIT

"""log command — tail task or system logs in real time."""

import subprocess

import click
from rich.console import Console

from claw_cron.executor import LOGS_DIR

console = Console()

SYSTEM_LOG = LOGS_DIR / "claw-cron.log"


@click.command()
@click.argument("name", required=False, default=None)
def log(name: str | None) -> None:
    """Tail task log or system log in real time.

    Without NAME: tail the system log (logs/claw-cron.log).
    With NAME: tail the named task's log (logs/<name>.log).
    """
    if name:
        log_path = LOGS_DIR / f"{name}.log"
    else:
        log_path = SYSTEM_LOG

    if not log_path.exists():
        console.print(f"[yellow]Log file not found: {log_path}[/yellow]")
        console.print("[dim]Run a task first to generate logs.[/dim]")
        raise SystemExit(1)

    console.print(f"[dim]Tailing: {log_path}[/dim]")
    console.print("[dim]Press Ctrl+C to stop.[/dim]\n")
    try:
        subprocess.run(["tail", "-f", str(log_path)])
    except KeyboardInterrupt:
        pass
```
</action>

<acceptance_criteria>
- `grep "def log" src/claw_cron/cmd/log.py` 有输出
- `grep "tail.*-f" src/claw_cron/cmd/log.py` 有输出
- `grep "SYSTEM_LOG" src/claw_cron/cmd/log.py` 有输出
- `grep "required=False" src/claw_cron/cmd/log.py` 有输出（name 参数可选）
- `uv run python -c "from claw_cron.cmd.log import log; print('ok')"` 输出 `ok`
</acceptance_criteria>

---

## Verification

```bash
uv run python -c "
from claw_cron.cmd.log import log, SYSTEM_LOG
import click
assert isinstance(log, click.BaseCommand)
assert 'claw-cron.log' in str(SYSTEM_LOG)
print('log command OK')
"
```

## must_haves

- `log` 无参数时跟踪系统日志 `logs/claw-cron.log`
- `log <name>` 跟踪任务日志 `logs/<name>.log`
- 日志文件不存在时输出友好提示并退出
- Ctrl+C 可正常退出（不抛出异常）
