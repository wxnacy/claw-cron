---
wave: 3
plan: "03-03"
title: "run Command — Execute Task by Name"
depends_on:
  - "03-02"
files_modified:
  - src/claw_cron/cmd/run.py
autonomous: true
requirements_addressed:
  - EXEC-01
  - EXEC-02
  - EXEC-03
---

# Plan 03-03: run Command

## Objective

实现 `claw-cron run <name>` 命令，按名称立即执行指定任务，输出执行结果和日志路径。

## Tasks

### Task 1: 创建 cmd/run.py

<read_first>
- `src/claw_cron/cmd/add.py` — Click 命令定义模式、Rich console 使用方式
- `src/claw_cron/executor.py` — execute_task 函数签名和 LOGS_DIR 路径
- `src/claw_cron/storage.py` — get_task 函数
</read_first>

<action>
创建 `src/claw_cron/cmd/run.py`：

```python
# SPDX-FileCopyrightText: 2026-present wxnacy <371032668@qq.com>
#
# SPDX-License-Identifier: MIT

"""run command — execute a task immediately by name."""

import click
from rich.console import Console

from claw_cron.executor import LOGS_DIR, execute_task
from claw_cron.storage import get_task

console = Console()


@click.command()
@click.argument("name")
def run(name: str) -> None:
    """Execute a task immediately by name."""
    task = get_task(name)
    if task is None:
        console.print(f"[red]Task '{name}' not found.[/red]")
        raise SystemExit(1)

    log_path = LOGS_DIR / f"{name}.log"
    console.print(f"[cyan]Running task '{name}'...[/cyan]")
    console.print(f"[dim]Log: {log_path}[/dim]")

    exit_code = execute_task(task)

    if exit_code == 0:
        console.print(f"[green]Task '{name}' completed successfully.[/green]")
    else:
        console.print(f"[red]Task '{name}' failed (exit_code={exit_code}).[/red]")
    raise SystemExit(exit_code)
```
</action>

<acceptance_criteria>
- `grep "def run" src/claw_cron/cmd/run.py` 有输出
- `grep "execute_task" src/claw_cron/cmd/run.py` 有输出
- `grep "get_task" src/claw_cron/cmd/run.py` 有输出
- `grep "SystemExit" src/claw_cron/cmd/run.py` 有输出（错误时退出码非零）
- `uv run python -c "from claw_cron.cmd.run import run; print('ok')"` 输出 `ok`
</acceptance_criteria>

---

## Verification

```bash
uv run python -c "
from claw_cron.cmd.run import run
import click
assert isinstance(run, click.BaseCommand)
print('run command OK')
"
```

## must_haves

- `run <name>` 任务不存在时输出错误并以非零退出码退出
- `run <name>` 执行前打印日志文件路径
- 执行结果（成功/失败）通过颜色区分输出
