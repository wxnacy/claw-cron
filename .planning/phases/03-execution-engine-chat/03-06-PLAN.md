---
wave: 5
plan: "03-06"
title: "CLI Registration — run/log/chat Commands"
depends_on:
  - "03-03"
  - "03-04"
  - "03-05"
files_modified:
  - src/claw_cron/cli.py
autonomous: true
requirements_addressed:
  - EXEC-01
  - EXEC-02
  - EXEC-03
  - CHAT-01
---

# Plan 03-06: CLI Registration

## Objective

在 `cli.py` 中注册 Phase 3 新增的三个命令：`run`、`log`、`chat`。

## Tasks

### Task 1: 更新 cli.py 注册新命令

<read_first>
- `src/claw_cron/cli.py` — 当前命令注册方式（import + cli.add_command 模式）
- `src/claw_cron/cmd/run.py` — run 命令（刚创建）
- `src/claw_cron/cmd/log.py` — log 命令（刚创建）
- `src/claw_cron/cmd/chat.py` — chat 命令（刚创建）
</read_first>

<action>
修改 `src/claw_cron/cli.py`，新增三个 import 和三个 `cli.add_command` 调用：

在现有 import 块末尾新增：
```python
from claw_cron.cmd.chat import chat
from claw_cron.cmd.log import log
from claw_cron.cmd.run import run
```

在现有 `cli.add_command(delete)` 之后新增：
```python
cli.add_command(run)
cli.add_command(log)
cli.add_command(chat)
```

完整 cli.py 结果：
```python
# SPDX-FileCopyrightText: 2026-present wxnacy <371032668@qq.com>
#
# SPDX-License-Identifier: MIT

import click

from claw_cron.__about__ import __version__
from claw_cron.cmd.add import add
from claw_cron.cmd.chat import chat
from claw_cron.cmd.delete import delete
from claw_cron.cmd.list import list_tasks
from claw_cron.cmd.log import log
from claw_cron.cmd.run import run


@click.group(context_settings={"help_option_names": ["-h", "--help"]})
@click.version_option(__version__, "-v", "--version")
def cli() -> None:
    """claw-cron: AI-powered cron task manager."""
    pass


cli.add_command(add)
cli.add_command(list_tasks)
cli.add_command(delete)
cli.add_command(run)
cli.add_command(log)
cli.add_command(chat)
```
</action>

<acceptance_criteria>
- `grep "from claw_cron.cmd.run import run" src/claw_cron/cli.py` 有输出
- `grep "from claw_cron.cmd.log import log" src/claw_cron/cli.py` 有输出
- `grep "from claw_cron.cmd.chat import chat" src/claw_cron/cli.py` 有输出
- `grep "cli.add_command(run)" src/claw_cron/cli.py` 有输出
- `grep "cli.add_command(log)" src/claw_cron/cli.py` 有输出
- `grep "cli.add_command(chat)" src/claw_cron/cli.py` 有输出
- `uv run claw-cron -h` 输出包含 `run`、`log`、`chat`
</acceptance_criteria>

---

## Verification

```bash
# 验证所有命令可见
uv run claw-cron -h | grep -E "run|log|chat"

# 验证各命令帮助可访问
uv run claw-cron run -h
uv run claw-cron log -h
uv run claw-cron chat -h
```

## must_haves

- `claw-cron -h` 输出包含 run、log、chat 三个命令
- 所有已有命令（add、list、delete）保持不变
- `uv run claw-cron run -h`、`log -h`、`chat -h` 均可正常输出帮助信息
