---
phase: 10-interactive-commands
plan: "02"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/claw_cron/cmd/delete.py
  - src/claw_cron/cmd/channels.py
  - src/claw_cron/cmd/chat.py
  - src/claw_cron/agent.py
autonomous: true
requirements:
  - INTERACT-04
user_setup: []
must_haves:
  truths:
    - "delete 命令使用 InquirerPy 确认"
    - "channels delete 命令使用 InquirerPy 确认"
    - "contacts delete 命令使用 InquirerPy 确认"
    - "chat 命令使用 InquirerPy 输入"
    - "agent.py 使用 InquirerPy 选择客户端"
  artifacts:
    - path: "src/claw_cron/cmd/delete.py"
      contains: "from claw_cron.prompt import prompt_confirm"
    - path: "src/claw_cron/cmd/channels.py"
      contains: "from claw_cron.prompt import prompt_confirm"
    - path: "src/claw_cron/cmd/chat.py"
      contains: "from claw_cron.prompt import prompt_text"
    - path: "src/claw_cron/agent.py"
      contains: "from claw_cron.prompt import"
  key_links:
    - from: "cmd/delete.py"
      to: "prompt.py"
      via: "import prompt_confirm"
      pattern: "prompt_confirm"
---

<objective>
将项目中现有的 `click.prompt`、`click.confirm` 调用替换为 InquirerPy 实现，保持统一的交互风格。

Purpose: 统一所有交互式调用的视觉风格，提供更好的用户体验
Output: 4 个文件中所有 click.prompt/confirm 调用替换为 prompt.py 函数
</objective>

<execution_context>
@${CODEBUDDY_PLUGIN_ROOT}/workflows/execute-plan.md
@${CODEBUDDY_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v2.2.md
@.planning/REQUIREMENTS-v2.2.md

# 现有代码 - 需要替换的位置
@src/claw_cron/cmd/delete.py — Line 24: click.confirm
@src/claw_cron/cmd/channels.py — Line 152, 321: click.confirm
@src/claw_cron/cmd/chat.py — Line 194: click.prompt
@src/claw_cron/agent.py — Line 124-127, 156: click.prompt
</context>

<interfaces>
<!-- 需要使用的 prompt.py 接口 -->
From src/claw_cron/prompt.py (10-01 创建):
- prompt_text(message: str, default: str | None = None) -> str
- prompt_confirm(message: str, default: bool = True) -> bool
- prompt_select(message: str, choices: list[str], default: str | None = None) -> str
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 替换 delete.py 中的 click.confirm</name>
  <files>src/claw_cron/cmd/delete.py</files>
  <read_first>
    - src/claw_cron/cmd/delete.py
    - src/claw_cron/prompt.py
  </read_first>
  <action>
修改 `src/claw_cron/cmd/delete.py`：

1. 添加导入：
```python
from claw_cron.prompt import prompt_confirm
```

2. 替换 Line 24 的 `click.confirm` 调用：
```python
# 替换前
click.confirm(f"Delete task '{name}'?", abort=True)

# 替换后
if not prompt_confirm(f"Delete task '{name}'?"):
    console.print("[dim]Cancelled.[/dim]")
    raise SystemExit(0)
```

注意：`click.confirm(abort=True)` 在用户选择 No 时会退出程序，替换后需要手动处理退出逻辑。
  </action>
  <acceptance_criteria>
    - 文件包含 `from claw_cron.prompt import prompt_confirm`
    - 不包含 `click.confirm` 调用
    - 用户选择 No 时程序正常退出
  </acceptance_criteria>
  <verify>
    <automated>grep -q "from claw_cron.prompt import prompt_confirm" src/claw_cron/cmd/delete.py && ! grep -q "click.confirm" src/claw_cron/cmd/delete.py</automated>
  </verify>
  <done>delete.py 使用 prompt_confirm 替换 click.confirm</done>
</task>

<task type="auto">
  <name>Task 2: 替换 channels.py 中的 click.confirm</name>
  <files>src/claw_cron/cmd/channels.py</files>
  <read_first>
    - src/claw_cron/cmd/channels.py
    - src/claw_cron/prompt.py
  </read_first>
  <action>
修改 `src/claw_cron/cmd/channels.py`：

1. 添加导入：
```python
from claw_cron.prompt import prompt_confirm
```

2. 替换 Line 152 的 `click.confirm` 调用（delete 命令中）：
```python
# 替换前
if not force:
    if not click.confirm(f"Delete channel '{channel_type}'?"):
        console.print("[dim]Cancelled.[/dim]")
        return

# 替换后
if not force:
    if not prompt_confirm(f"Delete channel '{channel_type}'?"):
        console.print("[dim]Cancelled.[/dim]")
        return
```

3. 替换 Line 321 的 `click.confirm` 调用（contacts delete 命令中）：
```python
# 替换前
if not force and not click.confirm(f"Delete contact '{alias}'?"):
    return

# 替换后
if not force and not prompt_confirm(f"Delete contact '{alias}'?"):
    return
```
  </action>
  <acceptance_criteria>
    - 文件包含 `from claw_cron.prompt import prompt_confirm`
    - 不包含 `click.confirm` 调用
  </acceptance_criteria>
  <verify>
    <automated>grep -q "from claw_cron.prompt import prompt_confirm" src/claw_cron/cmd/channels.py && ! grep -q "click.confirm" src/claw_cron/cmd/channels.py</automated>
  </verify>
  <done>channels.py 使用 prompt_confirm 替换 click.confirm</done>
</task>

<task type="auto">
  <name>Task 3: 替换 chat.py 中的 click.prompt</name>
  <files>src/claw_cron/cmd/chat.py</files>
  <read_first>
    - src/claw_cron/cmd/chat.py
    - src/claw_cron/prompt.py
  </read_first>
  <action>
修改 `src/claw_cron/cmd/chat.py`：

1. 添加导入：
```python
from claw_cron.prompt import prompt_text
```

2. 替换 Line 194 的 `click.prompt` 调用：
```python
# 替换前
user_input = click.prompt("You")

# 替换后
user_input = prompt_text("You")
```

注意：`click.prompt` 在 EOF/KeyboardInterrupt 时会抛出异常，`prompt_text` 需要处理相同情况或保持一致的异常行为。
  </action>
  <acceptance_criteria>
    - 文件包含 `from claw_cron.prompt import prompt_text`
    - 不包含 `click.prompt` 调用
  </acceptance_criteria>
  <verify>
    <automated>grep -q "from claw_cron.prompt import prompt_text" src/claw_cron/cmd/chat.py && ! grep -q "click.prompt" src/claw_cron/cmd/chat.py</automated>
  </verify>
  <done>chat.py 使用 prompt_text 替换 click.prompt</done>
</task>

<task type="auto">
  <name>Task 4: 替换 agent.py 中的 click.prompt</name>
  <files>src/claw_cron/agent.py</files>
  <read_first>
    - src/claw_cron/agent.py
    - src/claw_cron/prompt.py
  </read_first>
  <action>
修改 `src/claw_cron/agent.py`：

1. 添加导入：
```python
from claw_cron.prompt import prompt_select, prompt_text
```

2. 替换 Line 124-127 的 `click.prompt` 调用（选择 AI 客户端）：
```python
# 替换前
resolved_client = click.prompt(
    "Select AI client",
    type=click.Choice(["kiro-cli", "codebuddy", "opencode"]),
    default="kiro-cli",
)

# 替换后
resolved_client = prompt_select(
    "Select AI client",
    choices=["kiro-cli", "codebuddy", "opencode"],
    default="kiro-cli",
)
```

3. 替换 Line 156 的 `click.prompt` 调用（用户输入）：
```python
# 替换前
user_reply = click.prompt("\nYou")

# 替换后
user_reply = prompt_text("\nYou")
```
  </action>
  <acceptance_criteria>
    - 文件包含 `from claw_cron.prompt import`
    - 不包含 `click.prompt` 调用
    - 选择 AI 客户端使用 prompt_select
    - 用户输入使用 prompt_text
  </acceptance_criteria>
  <verify>
    <automated>grep -q "from claw_cron.prompt import" src/claw_cron/agent.py && ! grep -q "click.prompt" src/claw_cron/agent.py</automated>
  </verify>
  <done>agent.py 使用 prompt_select 和 prompt_text 替换 click.prompt</done>
</task>

</tasks>

<verification>
1. 运行 `grep -r "click.prompt\|click.confirm" src/claw_cron/` 应该没有结果（除了可能的测试文件）
2. 所有 4 个文件都导入了 prompt 模块
3. 运行 `python -m claw_cron delete test_task` 测试确认交互（需要先创建任务）
</verification>

<success_criteria>
- delete.py、channels.py、chat.py、agent.py 中所有 click.prompt/confirm 调用已替换
- 所有文件正确导入 prompt 模块
- 交互行为保持一致（确认/取消逻辑正确）
</success_criteria>

<output>
After completion, create `.planning/phases/10-interactive-commands/10-02-SUMMARY.md`
</output>
