---
phase: 10-interactive-commands
plan: "03"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/claw_cron/cmd/remind.py
autonomous: true
requirements:
  - INTERACT-02
user_setup: []
must_haves:
  truths:
    - "用户运行 claw-cron remind 无参数进入交互模式"
    - "交互模式分步引导填写 name, cron, message, channel, recipient"
    - "channel 从已配置通道列表选择"
    - "recipient 支持选择联系人或手动输入"
    - "cron 支持预设选择或自定义"
    - "最终确认后创建任务"
  artifacts:
    - path: "src/claw_cron/cmd/remind.py"
      contains: "_remind_interactive"
      exports: ["remind", "_remind_interactive"]
  key_links:
    - from: "cmd/remind.py"
      to: "prompt.py"
      via: "import prompt_*"
      pattern: "from claw_cron.prompt import"
    - from: "cmd/remind.py"
      to: "contacts.py"
      via: "load_contacts"
      pattern: "load_contacts"
---

<objective>
为 `remind` 命令添加交互式模式。当用户不传必选参数时，进入交互式引导流程，分步填写 name、cron、message、channel、recipient。

Purpose: 降低 remind 命令使用门槛，提供友好的引导式体验
Output: 更新 remind.py，支持交互式模式
</objective>

<execution_context>
@${CODEBUDDY_PLUGIN_ROOT}/workflows/execute-plan.md
@${CODEBUDDY_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v2.2.md
@.planning/REQUIREMENTS-v2.2.md

# 现有代码
@src/claw_cron/cmd/remind.py — 当前 remind 命令实现
@src/claw_cron/cmd/add.py — 参考：直接/交互模式切换逻辑
@src/claw_cron/prompt.py — 交互函数（10-01 创建）
@src/claw_cron/contacts.py — 联系人加载
@src/claw_cron/config.py — 通道配置加载
</context>

<interfaces>
<!-- 需要使用的接口 -->
From src/claw_cron/prompt.py:
- prompt_text(message: str, default: str | None = None) -> str
- prompt_confirm(message: str, default: bool = True) -> bool
- prompt_select(message: str, choices: list[str], default: str | None = None) -> str
- prompt_multiselect(message: str, choices: list[str]) -> list[str]
- prompt_cron() -> str

From src/claw_cron/contacts.py:
- load_contacts() -> dict[str, Contact]

From src/claw_cron/config.py:
- load_config() -> dict[str, Any]

From src/claw_cron/storage.py:
- Task 数据类
- add_task(task: Task) -> None

From src/claw_cron/notifier.py:
- NotifyConfig 数据类
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 修改 remind 命令参数为可选</name>
  <files>src/claw_cron/cmd/remind.py</files>
  <read_first>
    - src/claw_cron/cmd/remind.py
    - src/claw_cron/cmd/add.py (参考直接/交互模式切换)
  </read_first>
  <action>
修改 `src/claw_cron/cmd/remind.py`：

1. 将所有 `--name`, `--cron`, `--message`, `--channel`, `--recipient` 参数的 `required=True` 改为 `required=False` 或 `default=None`

2. 修改函数签名：
```python
def remind(
    name: str | None,
    cron: str | None,
    message: str | None,
    channel: str | None,
    recipients: tuple[str, ...] | None,
) -> None:
```

3. 添加交互模式判断逻辑（在函数开头）：
```python
# 检查是否进入交互模式
if not all([name, cron, message, channel, recipients]):
    return _remind_interactive()

# 直接模式：原有逻辑
...
```

参考 add.py 的模式切换逻辑（Line 46-54）。
  </action>
  <acceptance_criteria>
    - 所有参数的 required=True 已移除
    - 参数类型改为 str | None 或 tuple[str, ...] | None
    - 函数开头有交互模式判断逻辑
  </acceptance_criteria>
  <verify>
    <automated>grep -q "required=False" src/claw_cron/cmd/remind.py || grep -q "default=None" src/claw_cron/cmd/remind.py</automated>
  </verify>
  <done>remind 命令参数改为可选，添加交互模式入口判断</done>
</task>

<task type="auto">
  <name>Task 2: 实现 _remind_interactive 函数</name>
  <files>src/claw_cron/cmd/remind.py</files>
  <read_first>
    - src/claw_cron/cmd/remind.py
    - src/claw_cron/prompt.py
    - src/claw_cron/contacts.py
    - src/claw_cron/config.py
  </read_first>
  <action>
在 `src/claw_cron/cmd/remind.py` 中添加 `_remind_interactive` 函数：

```python
def _remind_interactive() -> None:
    """交互式引导创建 reminder 任务。"""
    console.print("[bold cyan]创建定时提醒[/bold cyan]\n")

    # 1. 输入任务名称
    name = prompt_text("提醒名称 (唯一标识)")

    # 2. 选择 Cron 表达式
    console.print("\n[bold]选择执行时间:[/bold]")
    cron = prompt_cron()

    # 3. 输入提醒消息
    message = prompt_text(
        "提醒内容 (支持 {{ date }}, {{ time }} 变量)"
    )

    # 4. 选择通知通道
    config = load_config()
    channels_config = config.get("channels", {})
    if not channels_config:
        console.print("[red]未配置通知通道。请先运行 'claw-cron channels add'[/red]")
        raise SystemExit(1)

    available_channels = list(channels_config.keys())
    if len(available_channels) == 1:
        channel = available_channels[0]
        console.print(f"[dim]使用通道: {channel}[/dim]")
    else:
        console.print("\n[bold]选择通知通道:[/bold]")
        channel = prompt_select("通道", choices=available_channels)

    # 5. 选择收件人
    contacts_data = load_contacts()
    channel_contacts = {
        alias: c for alias, c in contacts_data.items()
        if c.channel == channel
    }

    if channel_contacts:
        console.print("\n[bold]选择收件人:[/bold]")
        recipient_choices = list(channel_contacts.keys()) + ["[手动输入]"]
        selected = prompt_select("收件人", choices=recipient_choices)

        if selected == "[手动输入]":
            recipient_input = prompt_text(
                "输入收件人 (OpenID 格式如 'c2c:ABC123' 或 'group:XYZ789')"
            )
            recipients = [recipient_input]
        else:
            recipients = [selected]
    else:
        console.print("[dim]未找到已保存联系人[/dim]")
        recipient_input = prompt_text(
            "输入收件人 (OpenID 格式如 'c2c:ABC123' 或 'group:XYZ789')"
        )
        recipients = [recipient_input]

    # 6. 预览并确认
    console.print(f"\n[bold]确认创建提醒:[/bold]")
    console.print(f"  名称: {name}")
    console.print(f"  时间: {cron}")
    console.print(f"  内容: {message}")
    console.print(f"  通道: {channel}")
    console.print(f"  收件人: {', '.join(recipients)}")

    if not prompt_confirm("\n确认创建?", default=True):
        console.print("[dim]已取消[/dim]")
        return

    # 7. 创建任务
    resolved_recipients: list[str] = []
    for recipient in recipients:
        try:
            resolved = resolve_recipient(recipient, channel)
            resolved_recipients.append(resolved)
        except ValueError as e:
            console.print(f"[red]Error: {e}[/red]")
            raise SystemExit(1)

    notify = NotifyConfig(channel=channel, recipients=resolved_recipients)
    task = Task(
        name=name,
        cron=cron,
        type="reminder",
        message=message,
        notify=notify,
    )
    add_task(task)
    console.print(f"\n[green]提醒 '{name}' 创建成功[/green]")
```

需要添加的导入：
```python
from claw_cron.prompt import prompt_text, prompt_confirm, prompt_select, prompt_cron
from claw_cron.config import load_config
```
  </action>
  <acceptance_criteria>
    - 文件包含 `def _remind_interactive()` 函数
    - 函数包含 7 个步骤：名称、cron、消息、通道、收件人、确认、创建
    - 使用 prompt_cron() 选择 cron 表达式
    - 从已配置通道列表选择 channel
    - 从已保存联系人选择或手动输入 recipient
    - 最终确认后创建 Task
  </acceptance_criteria>
  <verify>
    <automated>grep -q "def _remind_interactive" src/claw_cron/cmd/remind.py && grep -q "prompt_cron" src/claw_cron/cmd/remind.py && grep -q "load_config" src/claw_cron/cmd/remind.py</automated>
  </verify>
  <done>_remind_interactive 函数实现完成，支持完整的交互流程</done>
</task>

</tasks>

<verification>
1. 运行 `python -m claw_cron remind --help` 确认参数变为可选
2. 运行 `python -m claw_cron remind`（无参数）应进入交互模式
3. grep 验证函数存在和导入正确
</verification>

<success_criteria>
- remind 命令无参数时进入交互模式
- 交互模式包含 name、cron、message、channel、recipient 引导
- channel 从已配置通道列表选择
- recipient 支持联系人选择或手动输入
- cron 使用预设选择功能
- 最终确认后创建任务
</success_criteria>

<output>
After completion, create `.planning/phases/10-interactive-commands/10-03-SUMMARY.md`
</output>
