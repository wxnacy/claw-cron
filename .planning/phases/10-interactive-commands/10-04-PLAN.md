---
phase: 10-interactive-commands
plan: "04"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/claw_cron/cmd/command.py
  - src/claw_cron/cli.py
  - skills/claw-cron/SKILL.md
autonomous: true
requirements:
  - INTERACT-03
  - INTERACT-06
user_setup: []
must_haves:
  truths:
    - "用户运行 claw-cron command 无参数进入交互模式"
    - "用户可以直接运行 claw-cron command --name x --cron '0 8 * * *' --script 'echo hi'"
    - "交互模式引导填写 name, cron, script"
    - "可选配置 notify (channel, recipient)"
    - "创建类型为 command 的 Task"
    - "SKILL.md 包含 command 命令说明"
  artifacts:
    - path: "src/claw_cron/cmd/command.py"
      provides: "command 命令实现"
      exports: ["command"]
    - path: "src/claw_cron/cli.py"
      contains: "cli.add_command(command)"
    - path: "skills/claw-cron/SKILL.md"
      contains: "claw-cron command"
  key_links:
    - from: "cli.py"
      to: "cmd/command.py"
      via: "import and add_command"
      pattern: "from claw_cron.cmd.command import command"
---

<objective>
新增 `command` 命令，专门用于创建 command 类型的定时任务。支持命令行直接模式和交互式模式两种使用方式。

Purpose: 简化 command 类型任务的创建流程，与 remind 命令保持一致的交互体验
Output: 新建 `src/claw_cron/cmd/command.py`，更新 CLI 和 SKILL.md
</objective>

<execution_context>
@${CODEBUDDY_PLUGIN_ROOT}/workflows/execute-plan.md
@${CODEBUDDY_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v2.2.md
@.planning/REQUIREMENTS-v2.2.md

# 参考代码
@src/claw_cron/cmd/remind.py — remind 命令实现（交互模式参考）
@src/claw_cron/cmd/add.py — 直接/交互模式切换参考
@src/claw_cron/cli.py — CLI 注册命令
@src/claw_cron/prompt.py — 交互函数
@skills/claw-cron/SKILL.md — 文档
</context>

<interfaces>
<!-- 需要使用的接口 -->
From src/claw_cron/prompt.py:
- prompt_text(message: str, default: str | None = None) -> str
- prompt_confirm(message: str, default: bool = True) -> bool
- prompt_select(message: str, choices: list[str], default: str | None = None) -> str
- prompt_cron() -> str

From src/claw_cron/storage.py:
- Task(name, cron, type, script, notify, ...)
- add_task(task: Task) -> None

From src/claw_cron/notifier.py:
- NotifyConfig(channel, recipients)

From src/claw_cron/contacts.py:
- load_contacts() -> dict[str, Contact]
- resolve_recipient(recipient, channel) -> str

From src/claw_cron/config.py:
- load_config() -> dict[str, Any]
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 command.py 命令模块</name>
  <files>src/claw_cron/cmd/command.py</files>
  <read_first>
    - src/claw_cron/cmd/remind.py (参考交互模式实现)
    - src/claw_cron/cmd/add.py (参考直接模式实现)
    - src/claw_cron/prompt.py (了解可用函数)
    - src/claw_cron/storage.py (了解 Task 结构)
  </read_first>
  <action>
创建 `src/claw_cron/cmd/command.py`：

```python
# SPDX-FileCopyrightText: 2026-present wxnacy <371032668@qq.com>
#
# SPDX-License-Identifier: MIT

"""command command — create a command-type scheduled task."""

from __future__ import annotations

import click
from rich.console import Console

from claw_cron.config import load_config
from claw_cron.contacts import load_contacts, resolve_recipient
from claw_cron.notifier import NotifyConfig
from claw_cron.prompt import prompt_text, prompt_confirm, prompt_select, prompt_cron
from claw_cron.storage import Task, add_task

console = Console()


@click.command()
@click.option("--name", default=None, help="Unique task name")
@click.option("--cron", default=None, help="Cron expression (e.g., '0 8 * * *')")
@click.option("--script", default=None, help="Shell command to execute")
@click.option("--channel", default=None, help="Notification channel (optional)")
@click.option(
    "--recipient",
    "recipients",
    multiple=True,
    default=None,
    help="Notification recipient (optional, can be repeated)",
)
def command(
    name: str | None,
    cron: str | None,
    script: str | None,
    channel: str | None,
    recipients: tuple[str, ...] | None,
) -> None:
    """Create a command-type scheduled task.

    Direct mode: provide --name, --cron, --script to create directly.
    Interactive mode: omit required options for guided creation.

    Examples:
        # Direct mode
        claw-cron command --name backup --cron "0 2 * * *" --script "backup.sh"

        # With notification
        claw-cron command --name health-check --cron "*/30 * * * *" \\
            --script "curl -s https://api.example.com/health" \\
            --channel qqbot --recipient me

        # Interactive mode
        claw-cron command
    """
    # Check if entering interactive mode
    if not all([name, cron, script]):
        return _command_interactive(name, cron, script, channel, recipients)

    # Direct mode
    _command_direct(name, cron, script, channel, recipients)


def _command_direct(
    name: str,
    cron: str,
    script: str,
    channel: str | None,
    recipients: tuple[str, ...] | None,
) -> None:
    """Create command task directly from arguments."""
    notify = None
    if channel and recipients:
        resolved_recipients: list[str] = []
        for recipient in recipients:
            try:
                resolved = resolve_recipient(recipient, channel)
                resolved_recipients.append(resolved)
            except ValueError as e:
                console.print(f"[red]Error: {e}[/red]")
                raise SystemExit(1)
        notify = NotifyConfig(channel=channel, recipients=resolved_recipients)

    task = Task(
        name=name,
        cron=cron,
        type="command",
        script=script,
        notify=notify,
    )
    add_task(task)
    console.print(f"[green]Command task '{name}' created.[/green]")
    console.print(f"[dim]  Cron: {cron}[/dim]")
    console.print(f"[dim]  Script: {script}[/dim]")
    if notify:
        console.print(f"[dim]  Notify: {channel} -> {', '.join(recipients or [])}[/dim]")


def _command_interactive(
    name: str | None = None,
    cron: str | None = None,
    script: str | None = None,
    channel: str | None = None,
    recipients: tuple[str, ...] | None = None,
) -> None:
    """Interactive guided creation for command task."""
    console.print("[bold cyan]创建命令任务[/bold cyan]\n")

    # 1. Task name
    if not name:
        name = prompt_text("任务名称 (唯一标识)")
    else:
        console.print(f"[dim]名称: {name}[/dim]")

    # 2. Cron expression
    if not cron:
        console.print("\n[bold]选择执行时间:[/bold]")
        cron = prompt_cron()
    else:
        console.print(f"[dim]时间: {cron}[/dim]")

    # 3. Script
    if not script:
        script = prompt_text("Shell 命令")

    # 4. Optional notification
    notify = None
    if prompt_confirm("\n配置执行通知?", default=False):
        config = load_config()
        channels_config = config.get("channels", {})

        if not channels_config:
            console.print("[yellow]未配置通知通道，跳过通知设置[/yellow]")
        else:
            available_channels = list(channels_config.keys())
            if len(available_channels) == 1:
                selected_channel = available_channels[0]
            else:
                console.print("\n[bold]选择通知通道:[/bold]")
                selected_channel = prompt_select("通道", choices=available_channels)

            # Select recipient
            contacts_data = load_contacts()
            channel_contacts = {
                alias: c for alias, c in contacts_data.items()
                if c.channel == selected_channel
            }

            if channel_contacts:
                console.print("\n[bold]选择收件人:[/bold]")
                recipient_choices = list(channel_contacts.keys()) + ["[手动输入]"]
                selected = prompt_select("收件人", choices=recipient_choices)

                if selected == "[手动输入]":
                    recipient_input = prompt_text(
                        "输入收件人 (OpenID 格式如 'c2c:ABC123')"
                    )
                    selected_recipients = [recipient_input]
                else:
                    selected_recipients = [selected]
            else:
                recipient_input = prompt_text(
                    "输入收件人 (OpenID 格式如 'c2c:ABC123')"
                )
                selected_recipients = [recipient_input]

            # Resolve recipients
            resolved: list[str] = []
            for r in selected_recipients:
                try:
                    resolved.append(resolve_recipient(r, selected_channel))
                except ValueError as e:
                    console.print(f"[red]Error: {e}[/red]")
                    raise SystemExit(1)

            notify = NotifyConfig(channel=selected_channel, recipients=resolved)

    # 5. Preview and confirm
    console.print(f"\n[bold]确认创建任务:[/bold]")
    console.print(f"  名称: {name}")
    console.print(f"  时间: {cron}")
    console.print(f"  命令: {script}")
    if notify:
        console.print(f"  通知: {notify.channel}")

    if not prompt_confirm("\n确认创建?", default=True):
        console.print("[dim]已取消[/dim]")
        return

    # 6. Create task
    task = Task(
        name=name,
        cron=cron,
        type="command",
        script=script,
        notify=notify,
    )
    add_task(task)
    console.print(f"\n[green]任务 '{name}' 创建成功[/green]")
```
  </action>
  <acceptance_criteria>
    - 文件存在 src/claw_cron/cmd/command.py
    - 包含 command 函数（Click 命令）
    - 包含 _command_direct 函数
    - 包含 _command_interactive 函数
    - 参数：--name, --cron, --script (必填), --channel, --recipient (可选)
    - 支持直接模式和交互模式
  </acceptance_criteria>
  <verify>
    <automated>test -f src/claw_cron/cmd/command.py && grep -q "def command" src/claw_cron/cmd/command.py && grep -q "def _command_direct" src/claw_cron/cmd/command.py && grep -q "def _command_interactive" src/claw_cron/cmd/command.py</automated>
  </verify>
  <done>command.py 模块创建完成，支持直接和交互两种模式</done>
</task>

<task type="auto">
  <name>Task 2: 注册 command 命令到 CLI</name>
  <files>src/claw_cron/cli.py</files>
  <read_first>
    - src/claw_cron/cli.py
  </read_first>
  <action>
修改 `src/claw_cron/cli.py`：

1. 添加导入（在其他 cmd 导入附近）：
```python
from claw_cron.cmd.command import command
```

2. 注册命令（在其他 cli.add_command 调用附近）：
```python
cli.add_command(command)
```

注意：保持与其他命令导入/注册的顺序一致。
  </action>
  <acceptance_criteria>
    - cli.py 包含 `from claw_cron.cmd.command import command`
    - cli.py 包含 `cli.add_command(command)`
    - 运行 `python -m claw_cron command --help` 显示帮助
  </acceptance_criteria>
  <verify>
    <automated>grep -q "from claw_cron.cmd.command import command" src/claw_cron/cli.py && grep -q "cli.add_command(command)" src/claw_cron/cli.py && python -m claw_cron command --help</automated>
  </verify>
  <done>command 命令已注册到 CLI</done>
</task>

<task type="auto">
  <name>Task 3: 更新 SKILL.md 文档</name>
  <files>skills/claw-cron/SKILL.md</files>
  <read_first>
    - skills/claw-cron/SKILL.md
  </read_first>
  <action>
更新 `skills/claw-cron/SKILL.md`：

1. 在 Commands 表格中添加 command 命令：
```markdown
| `claw-cron command` | Create a command-type task |
```

2. 在 Quick Start 添加 command 示例（在 add 示例后）：
```bash
# Interactive command creation
claw-cron command

# Direct command creation
claw-cron command --name backup --cron "0 2 * * *" --script "backup.sh"
```

3. 在 Examples 部分的 "Command Tasks" 后添加交互式说明：
```markdown
### Interactive Command Creation

```bash
# Interactive mode - guided prompts
claw-cron command

# The wizard will guide you through:
# 1. Task name
# 2. Cron schedule (with preset options)
# 3. Shell command
# 4. Optional notification setup
```

4. 更新 remind 命令说明，添加交互式模式：
```markdown
| `claw-cron remind` | Create a reminder (supports interactive mode) |
```

在 Reminders 部分添加：
```markdown
# Interactive reminder creation
claw-cron remind
```
  </action>
  <acceptance_criteria>
    - SKILL.md 包含 `claw-cron command` 命令说明
    - Commands 表格包含 command 条目
    - Quick Start 包含 command 示例
    - remind 命令说明包含交互式模式
  </acceptance_criteria>
  <verify>
    <automated>grep -q "claw-cron command" skills/claw-cron/SKILL.md && grep -q "Interactive mode" skills/claw-cron/SKILL.md</automated>
  </verify>
  <done>SKILL.md 文档已更新，包含 command 命令和交互式模式说明</done>
</task>

</tasks>

<verification>
1. 运行 `python -m claw_cron command --help` 显示帮助
2. 运行 `python -m claw_cron command --name test --cron "0 8 * * *" --script "echo hi"` 创建任务
3. 运行 `python -m claw_cron command` 进入交互模式
4. grep 验证 SKILL.md 包含 command 命令说明
</verification>

<success_criteria>
- command 命令可通过 `claw-cron command` 运行
- 无参数时进入交互模式
- 直接模式正确创建 command 类型任务
- 可选的通知配置正常工作
- SKILL.md 包含 command 命令文档
</success_criteria>

<output>
After completion, create `.planning/phases/10-interactive-commands/10-04-SUMMARY.md`
</output>
