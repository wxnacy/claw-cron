---
plan: 17-02
phase: 17
title: Documentation Update + Version Bump
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
  - pyproject.toml
  - src/claw_cron/__about__.py
autonomous: true
requirements_addressed:
  - VERS-01
---

# Plan 17-02: Documentation Update + Version Bump

## Objective

更新 README.md（新增企业微信通道说明 + capture 交互改进）、新建 CHANGELOG.md（记录 v0.2.1 变更）、升级版本号到 0.2.1。

## Context

<context>
- `src/claw_cron/__about__.py` — 当前版本 "0.1.0"，需升级到 "0.2.1"
- `pyproject.toml` — 使用 `dynamic = ["version"]`，版本由 `__about__.py` 驱动，无需修改版本字段
- `README.md` — 需新增企业微信通道配置说明和 capture 交互改进说明
- `.planning/phases/16-wechat-channel/16-CONTEXT.md` — 企业微信通道实现细节
- `.planning/phases/15-capture-interaction/15-CONTEXT.md` — capture 交互改进细节
</context>

## Tasks

### Task 1: 升级版本号

<read_first>
- `src/claw_cron/__about__.py`
- `pyproject.toml`
</read_first>

<action>
修改 `src/claw_cron/__about__.py`：
- 将 `__version__ = "0.1.0"` 改为 `__version__ = "0.2.1"`

`pyproject.toml` 使用 `dynamic = ["version"]` + hatchling，版本由 `__about__.py` 驱动，无需修改。
</action>

<acceptance_criteria>
- `grep '__version__' src/claw_cron/__about__.py` 输出 `__version__ = "0.2.1"`
- `python -c "from claw_cron.__about__ import __version__; print(__version__)"` 输出 `0.2.1`
</acceptance_criteria>

---

### Task 2: 新建 CHANGELOG.md

<read_first>
- `.planning/phases/14-architecture-enhancement/14-CONTEXT.md`
- `.planning/phases/15-capture-interaction/15-CONTEXT.md`
- `.planning/phases/16-wechat-channel/16-CONTEXT.md`
</read_first>

<action>
新建 `CHANGELOG.md`，内容如下（精确格式）：

```markdown
# Changelog

All notable changes to this project will be documented in this file.

## [0.2.1] - 2026-04-17

### Added
- 企业微信应用消息通知通道 (WeChat Work / 企业微信)
  - 支持文本消息和 Markdown 消息
  - 自动管理 access_token 生命周期（获取、缓存、到期前刷新）
  - 支持通过 `channels capture` 获取企业微信 userid
- `channels capture` 交互式列表选择通道类型（替代 `--channel-type` 参数）
- `channels add` 验证成功后自动询问是否执行 capture
- capture 流程实时状态反馈（Rich console.status）
- capture 流程 5 分钟超时机制，超时后给出明确错误提示
- `MessageChannel.supports_capture` 属性标识通道是否支持 capture
- `MessageChannel.capture_openid()` 抽象方法，支持通道特定 capture 实现

### Changed
- `channels capture` 移除 `--channel-type` 参数，改为交互式选择
- `channels add` 移除 `--capture-openid` 参数，改为 add 成功后自动询问

## [0.1.0] - 2026-04-16

### Added
- 初始版本：iMessage、QQ Bot、飞书、邮件通道
- `channels add/list/delete/verify/capture` 命令
- 联系人别名管理 (`channels contacts`)
- AI 驱动的定时任务配置
```
</action>

<acceptance_criteria>
- `test -f CHANGELOG.md` 退出码为 0
- `grep "\[0.2.1\]" CHANGELOG.md` 有输出
- `grep "企业微信" CHANGELOG.md` 有输出
- `grep "supports_capture" CHANGELOG.md` 有输出
- `grep "\[0.1.0\]" CHANGELOG.md` 有输出
</acceptance_criteria>

---

### Task 3: 更新 README.md

<read_first>
- `README.md`
- `src/claw_cron/cmd/channels.py`
</read_first>

<action>
在 README.md 中新增以下内容（如果 README.md 不存在则创建；如果存在则在合适位置追加）：

**新增章节：企业微信通道配置**

在通道配置部分（或文件末尾）添加：

```markdown
## 企业微信通道 (WeChat Work)

### 配置

```bash
claw-cron channels add
# 选择 wecom，然后输入：
# Corp ID: 你的企业 ID（企业微信管理后台 → 我的企业 → 企业信息）
# Agent ID: 应用 ID（企业微信管理后台 → 应用管理 → 自建应用）
# Secret: 应用 Secret
```

### 获取用户 ID

```bash
claw-cron channels capture
# 选择 wecom，然后在企业微信管理后台查找你的 userid 并输入
```

### 发送消息

```bash
claw-cron remind "每天 9 点提醒我开会" --recipient me
```

## Capture 交互改进 (v0.2.1)

`channels capture` 现在支持交互式选择通道类型，无需记忆 `--channel-type` 参数：

```bash
claw-cron channels capture
# 交互式列表选择通道类型
# 对不支持 capture 的通道（iMessage、邮件）给出友好提示
```

`channels add` 验证成功后会自动询问是否执行 capture（适用于 QQ Bot、飞书、企业微信）。
```
</action>

<acceptance_criteria>
- `grep "企业微信" README.md` 有输出
- `grep "Corp ID" README.md` 有输出
- `grep "capture" README.md` 有输出（至少 2 处）
- `grep "0.2.1" README.md` 有输出
</acceptance_criteria>

## Verification

```bash
# 版本号验证
python -c "from claw_cron.__about__ import __version__; assert __version__ == '0.2.1', __version__"

# 文件存在验证
test -f CHANGELOG.md && echo "CHANGELOG OK"
grep "\[0.2.1\]" CHANGELOG.md
grep "企业微信" README.md
```

## Must-Haves

- [ ] `src/claw_cron/__about__.py` 中 `__version__ == "0.2.1"`
- [ ] `CHANGELOG.md` 存在，包含 `[0.2.1]` 条目
- [ ] `README.md` 包含企业微信通道配置说明
- [ ] `README.md` 包含 capture 交互改进说明
