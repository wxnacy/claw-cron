---
plan: 15-01
phase: 15
name: Capture Interaction
wave: 1
depends_on: []
files_modified:
  - src/claw_cron/prompt.py
  - src/claw_cron/cmd/channels.py
requirements_addressed:
  - CAPT-01
  - CAPT-02
  - CAPT-03
  - CAPT-04
  - CAPT-05
autonomous: true
---

# Plan 15-01: Capture Interaction

## Objective

改进 capture 命令交互体验：
1. 移除 `--channel-type` 参数，改为交互式列表（仅显示支持 capture 的通道）
2. 移除 `add` 命令的 `--capture-openid` 参数，改为 add 成功后自动询问
3. 优化 capture 状态反馈（包含通道友好名称的引导提示）
4. 超时后显示带重试建议的错误提示
5. 不支持 capture 的通道显示友好提示

## Tasks

### Task 1: 新增 `prompt_capture_channel_select()` 函数

<read_first>
- src/claw_cron/prompt.py — 查看 prompt_channel_select() 实现，复用 InquirerPy 模式
- src/claw_cron/channels/__init__.py — 查看 CHANNEL_REGISTRY 和 get_channel_status()
- src/claw_cron/channels/base.py — 查看 supports_capture 属性
</read_first>

<action>
在 `src/claw_cron/prompt.py` 末尾添加 `prompt_capture_channel_select()` 函数：

```python
def prompt_capture_channel_select() -> str:
    """Prompt user to select a capture-capable channel with status indicators.

    Filters CHANNEL_REGISTRY to only show channels where supports_capture is True.

    Returns:
        The selected channel identifier string.
    """
    from claw_cron.channels import get_channel

    choices = []
    for channel_id in sorted(CHANNEL_REGISTRY.keys()):
        ch = get_channel(channel_id)
        if not ch.supports_capture:
            continue
        icon, status_text = get_channel_status(channel_id)
        name = f"{channel_id} ({status_text} {icon})"
        choices.append(Choice(value=channel_id, name=name))

    if not choices:
        raise ValueError("没有支持 capture 的通道")

    return inquirer.select(
        message="选择通道类型:",
        choices=choices,
    ).execute()
```
</action>

<acceptance_criteria>
- src/claw_cron/prompt.py 包含 `def prompt_capture_channel_select()`
- 函数仅对 `supports_capture == True` 的通道创建 Choice（qqbot、feishu 出现，imessage、email 不出现）
- 函数在无支持 capture 的通道时抛出 `ValueError`
</acceptance_criteria>

---

### Task 2: 重构 `capture` 命令 — 移除 `--channel-type`，改为交互式选择

<read_first>
- src/claw_cron/cmd/channels.py — 查看当前 capture() 函数（第 447-502 行），了解现有逻辑
- src/claw_cron/channels/base.py — 查看 capture_openid(timeout=300) 签名
- src/claw_cron/channels/exceptions.py — 查看 ChannelError, ChannelConfigError
</read_first>

<action>
将 `src/claw_cron/cmd/channels.py` 中的 `capture` 命令替换为以下实现：

1. 移除 `@click.option("--channel-type", ...)` 装饰器
2. 添加 `from claw_cron.prompt import prompt_capture_channel_select` 导入（在函数内部）
3. 新增通道友好名称映射：
   ```python
   CHANNEL_DISPLAY_NAMES = {
       "qqbot": "QQ Bot",
       "feishu": "飞书",
       "wechat": "企业微信",
   }
   ```
   放在文件顶部（`console = Console()` 之后）
4. 新的 `capture` 命令签名：
   ```python
   @channels.command()
   @click.option("--alias", prompt="Save as contact alias", default="me", help="Alias name for the captured contact")
   def capture(alias: str) -> None:
   ```
5. 函数体：
   ```python
   from claw_cron.channels import get_channel
   from claw_cron.channels.exceptions import ChannelConfigError, ChannelError
   from claw_cron.prompt import prompt_capture_channel_select

   channel_type = prompt_capture_channel_select()
   channel = get_channel(channel_type)
   display_name = CHANNEL_DISPLAY_NAMES.get(channel_type, channel_type)

   if not channel.supports_capture:
       # 防御性检查（正常流程不会到达此处）
       reasons = {
           "imessage": "iMessage 使用手机号直接发送，无需获取用户 ID",
           "email": "邮件使用邮箱地址直接发送",
       }
       reason = reasons.get(channel_type, "此通道不需要 capture")
       console.print(f"[yellow]此通道不需要 capture。{reason}[/yellow]")
       return

   console.print(f"\n[dim]请向你的 {display_name} 机器人发送任意消息[/dim]")
   console.print("[dim]按 Ctrl+C 取消[/dim]\n")

   try:
       with console.status(f"[bold green]等待来自 {display_name} 的消息..."):
           openid = asyncio.run(channel.capture_openid())
       console.print(f"[green]✓ OpenID 已捕获: [bold]{openid}[/bold][/green]")

       contact = Contact(
           openid=openid,
           channel=channel_type,
           alias=alias,
           created=datetime.now().isoformat(),
       )
       save_contact(contact)
       console.print(f"[green]✓ 联系人已保存为 '[bold]{alias}[/bold]'[/green]")
       console.print(f"[dim]现在可以使用 'claw-cron remind --recipient {alias}'[/dim]")
   except KeyboardInterrupt:
       console.print("\n[yellow]已取消[/yellow]")
   except ChannelError as e:
       # qqbot/feishu 的 capture_openid 将 asyncio.TimeoutError 包装为 ChannelError
       # 超时消息格式: "Capture timed out after 300s"
       if "timed out" in str(e).lower():
           console.print(f"\n[red]✗ Capture 超时（5 分钟），请确认机器人在线后重试[/red]")
       else:
           console.print(f"[red]Capture 失败: {e}[/red]")
       raise SystemExit(1)
   except ChannelConfigError as e:
       console.print(f"[red]Capture 失败: {e}[/red]")
       raise SystemExit(1)
   ```
</action>

<acceptance_criteria>
- `capture` 命令不再有 `--channel-type` 参数（`claw-cron channels capture --help` 输出中不含 `--channel-type`）
- `capture` 命令仍有 `--alias` 参数
- `CHANNEL_DISPLAY_NAMES` 字典存在于文件顶部
- 函数体调用 `prompt_capture_channel_select()` 而非接受 `channel_type` 参数
- 超时错误消息包含 "5 分钟" 和 "重试"
- 防御性检查：不支持 capture 的通道显示 "此通道不需要 capture。{reason}"
</acceptance_criteria>

---

### Task 3: 重构 `add` 命令 — 移除 `--capture-openid`，改为自动询问

<read_first>
- src/claw_cron/cmd/channels.py — 查看 add() 函数（第 39-217 行），特别是 qqbot 和 feishu 的 capture_openid 分支
- src/claw_cron/channels/base.py — 查看 supports_capture 属性
- src/claw_cron/prompt.py — 查看 prompt_confirm() 函数签名
</read_first>

<action>
修改 `src/claw_cron/cmd/channels.py` 中的 `add` 命令：

1. 移除 `@click.option("--capture-openid", ...)` 装饰器
2. 修改函数签名为 `def add() -> None:`
3. 提取公共 capture 逻辑为内部辅助函数（在 add 函数内部定义）：
   ```python
   def _do_capture(ch_type: str) -> None:
       from claw_cron.channels import get_channel
       from claw_cron.channels.exceptions import ChannelConfigError, ChannelError
       display_name = CHANNEL_DISPLAY_NAMES.get(ch_type, ch_type)
       ch = get_channel(ch_type)
       console.print(f"\n[dim]请向你的 {display_name} 机器人发送任意消息[/dim]")
       console.print("[dim]按 Ctrl+C 取消[/dim]\n")
       try:
           with console.status(f"[bold green]等待来自 {display_name} 的消息..."):
               openid = asyncio.run(ch.capture_openid())
           console.print(f"[green]✓ OpenID 已捕获: [bold]{openid}[/bold][/green]")
           save_contact(Contact(openid=openid, channel=ch_type, alias="me", created=datetime.now().isoformat()))
           console.print("[green]✓ 联系人已保存为 'me'[/green]")
       except KeyboardInterrupt:
           console.print("\n[yellow]已取消[/yellow]")
       except ChannelError as e:
           # qqbot/feishu 将 asyncio.TimeoutError 包装为 ChannelError("Capture timed out after 300s")
           if "timed out" in str(e).lower():
               console.print(f"\n[red]✗ Capture 超时（5 分钟），请确认机器人在线后重试[/red]")
           else:
               console.print(f"[red]Capture 失败: {e}[/red]")
       except ChannelConfigError as e:
           console.print(f"[red]Capture 失败: {e}[/red]")
   ```
4. 在 qqbot 配置保存成功后（`console.print(f"[green]✓ 通道 'qqbot' 配置完成[/green]")` 之后），替换原有 `if capture_openid:` 块为：
   ```python
   if prompt_confirm("是否立即获取用户 ID (capture)?"):
       _do_capture("qqbot")
   ```
5. 在 feishu 配置保存成功后，同样替换原有 `if capture_openid:` 块为：
   ```python
   if prompt_confirm("是否立即获取用户 ID (capture)?"):
       _do_capture("feishu")
   ```
6. email 和 imessage 分支不添加 capture 询问（这两个通道 `supports_capture=False`）
</action>

<acceptance_criteria>
- `add` 命令不再有 `--capture-openid` 参数（`claw-cron channels add --help` 输出中不含 `--capture-openid`）
- qqbot 配置成功后调用 `prompt_confirm("是否立即获取用户 ID (capture)?")`
- feishu 配置成功后调用 `prompt_confirm("是否立即获取用户 ID (capture)?")`
- email 和 imessage 分支不含 capture 询问逻辑
- `_do_capture` 内部函数使用 `CHANNEL_DISPLAY_NAMES` 获取友好名称
- `_do_capture` 内部函数的 status 文字格式为 "等待来自 {display_name} 的消息..."
</acceptance_criteria>

---

## Verification

```bash
# 验证 prompt.py 新增函数
grep -n "def prompt_capture_channel_select" src/claw_cron/prompt.py

# 验证 capture 命令不再有 --channel-type
python -m claw_cron channels capture --help 2>&1 | grep -v "channel-type"

# 验证 add 命令不再有 --capture-openid
python -m claw_cron channels add --help 2>&1 | grep -v "capture-openid"

# 验证 CHANNEL_DISPLAY_NAMES 存在
grep -n "CHANNEL_DISPLAY_NAMES" src/claw_cron/cmd/channels.py

# 验证超时错误消息
grep -n "5 分钟" src/claw_cron/cmd/channels.py

# 验证自动询问逻辑
grep -n "是否立即获取用户 ID" src/claw_cron/cmd/channels.py
```

## Must-Haves

- [ ] `channels capture` 不再需要 `--channel-type` 参数，改为交互式选择（CAPT-01）
- [ ] 不支持 capture 的通道显示友好提示（CAPT-02）
- [ ] `channels add` 成功后自动询问是否执行 capture（CAPT-03）
- [ ] capture 等待时显示包含通道名的 spinner 状态（CAPT-04）
- [ ] capture 超时后显示带重试建议的错误提示（CAPT-05）
