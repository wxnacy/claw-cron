---
plan: 13-01
phase: 13
wave: 1
title: Core EmailChannel Implementation
depends_on: []
files_modified:
  - src/claw_cron/channels/email.py
autonomous: true
requirements_addressed:
  - EMAIL-01
  - EMAIL-02
  - EMAIL-03
  - EMAIL-04
  - EMAIL-05
---

# Plan 13-01: Core EmailChannel Implementation

## Objective

实现 `EmailChannel` 类，使用 `aiosmtplib` 进行异步 SMTP 发送，支持纯文本、HTML（Markdown 转换）和附件。

## Context

<context>
- `src/claw_cron/channels/base.py` — MessageChannel 抽象基类，ChannelConfig，MessageResult
- `src/claw_cron/channels/feishu.py` — 参考实现（配置类模式、错误处理、_validate_config）
- `src/claw_cron/channels/exceptions.py` — ChannelConfigError, ChannelSendError, ChannelAuthError
</context>

## Tasks

### Task 1: Create EmailConfig

<read_first>
- `src/claw_cron/channels/feishu.py` — FeishuConfig 模式（pydantic-settings, env_prefix, Field）
- `src/claw_cron/channels/base.py` — ChannelConfig 基类
</read_first>

<action>
创建 `src/claw_cron/channels/email.py`，实现 `EmailConfig`：

```python
class EmailConfig(BaseSettings, ChannelConfig):
    host: str | None = Field(default=None, description="SMTP server host")
    port: int = Field(default=587, description="SMTP port")
    username: str | None = Field(default=None, description="SMTP username")
    password: str | None = Field(default=None, description="SMTP password")
    from_email: str | None = Field(default=None, description="Sender email address")
    use_tls: bool = Field(default=True, description="Use STARTTLS")

    class Config:
        env_prefix = "CLAW_CRON_EMAIL_"
        env_file = ".env"
        extra = "ignore"
```
</action>

<acceptance_criteria>
- `src/claw_cron/channels/email.py` 存在
- 文件包含 `class EmailConfig(BaseSettings, ChannelConfig):`
- 文件包含 `env_prefix = "CLAW_CRON_EMAIL_"`
- 字段包含 `host`, `port`, `username`, `password`, `from_email`, `use_tls`
- `port` 默认值为 `587`，`use_tls` 默认值为 `True`
</acceptance_criteria>

---

### Task 2: Implement EmailChannel._validate_config and channel_id

<read_first>
- `src/claw_cron/channels/email.py` — 刚创建的文件（当前状态）
- `src/claw_cron/channels/feishu.py` — `_validate_config` 和 `channel_id` 模式
- `src/claw_cron/channels/exceptions.py` — ChannelConfigError
</read_first>

<action>
在 `EmailChannel` 类中实现：

```python
class EmailChannel(MessageChannel):
    def __init__(self, config: EmailConfig | dict | None = None) -> None:
        if config is None:
            config_obj = EmailConfig()
        elif isinstance(config, dict):
            config_obj = EmailConfig(**config)
        else:
            config_obj = config
        super().__init__(config_obj)

    @property
    def channel_id(self) -> str:
        return "email"

    def _validate_config(self) -> None:
        required = ["host", "username", "password", "from_email"]
        for field in required:
            if not getattr(self.config, field, None):
                raise ChannelConfigError(
                    f"Email {field} is required. Set CLAW_CRON_EMAIL_{field.upper()} environment variable.",
                    channel_id=self.channel_id,
                )
```
</action>

<acceptance_criteria>
- `email.py` 包含 `class EmailChannel(MessageChannel):`
- `channel_id` 属性返回 `"email"`
- `_validate_config` 检查 `host`, `username`, `password`, `from_email` 四个字段
- 缺少字段时抛出 `ChannelConfigError`
</acceptance_criteria>

---

### Task 3: Implement send_text

<read_first>
- `src/claw_cron/channels/email.py` — 当前状态
- `src/claw_cron/channels/base.py` — `send_text` 签名：`async def send_text(self, recipient: str, content: str) -> MessageResult`
</read_first>

<action>
实现 `send_text`，使用 `aiosmtplib` 发送纯文本邮件：

```python
async def send_text(self, recipient: str, content: str) -> MessageResult:
    self._validate_config()
    recipients = [r.strip() for r in recipient.split(",")]
    msg = MIMEText(content, "plain", "utf-8")
    msg["Subject"] = content[:50]  # 使用内容前50字符作为主题
    msg["From"] = self.config.from_email
    msg["To"] = ", ".join(recipients)

    try:
        await aiosmtplib.send(
            msg,
            hostname=self.config.host,
            port=self.config.port,
            username=self.config.username,
            password=self.config.password,
            start_tls=self.config.use_tls,
        )
        return MessageResult(success=True)
    except aiosmtplib.SMTPAuthenticationError as e:
        raise ChannelAuthError(str(e), channel_id=self.channel_id)
    except aiosmtplib.SMTPException as e:
        return MessageResult(success=False, error=str(e))
```

导入：`import aiosmtplib`, `from email.mime.text import MIMEText`
</action>

<acceptance_criteria>
- `email.py` 包含 `import aiosmtplib`
- `email.py` 包含 `from email.mime.text import MIMEText`
- `send_text` 方法存在且为 `async def`
- `send_text` 调用 `aiosmtplib.send(`
- 多收件人：`recipient.split(",")` 处理逗号分隔
- 返回 `MessageResult(success=True)` 或 `MessageResult(success=False, error=...)`
</acceptance_criteria>

---

### Task 4: Implement send_markdown (Markdown → HTML)

<read_first>
- `src/claw_cron/channels/email.py` — 当前状态
- `src/claw_cron/channels/base.py` — `send_markdown` 签名
</read_first>

<action>
实现 `send_markdown`，使用 `markdown` 库转换为 HTML，发送 `multipart/alternative` 邮件：

```python
async def send_markdown(self, recipient: str, content: str) -> MessageResult:
    self._validate_config()
    import markdown as md_lib
    recipients = [r.strip() for r in recipient.split(",")]
    html_content = md_lib.markdown(content)

    msg = MIMEMultipart("alternative")
    msg["Subject"] = content.split("\n")[0].lstrip("# ")[:50]
    msg["From"] = self.config.from_email
    msg["To"] = ", ".join(recipients)
    msg.attach(MIMEText(content, "plain", "utf-8"))
    msg.attach(MIMEText(html_content, "html", "utf-8"))

    try:
        await aiosmtplib.send(
            msg,
            hostname=self.config.host,
            port=self.config.port,
            username=self.config.username,
            password=self.config.password,
            start_tls=self.config.use_tls,
        )
        return MessageResult(success=True)
    except aiosmtplib.SMTPAuthenticationError as e:
        raise ChannelAuthError(str(e), channel_id=self.channel_id)
    except aiosmtplib.SMTPException as e:
        return MessageResult(success=False, error=str(e))
```

导入：`from email.mime.multipart import MIMEMultipart`
</action>

<acceptance_criteria>
- `email.py` 包含 `from email.mime.multipart import MIMEMultipart`
- `send_markdown` 方法存在且为 `async def`
- `send_markdown` 调用 `md_lib.markdown(content)` 或 `markdown.markdown(content)`
- `send_markdown` 创建 `MIMEMultipart("alternative")`
- `msg.attach` 被调用两次（plain + html）
</acceptance_criteria>

---

### Task 5: Implement send_text with attachments support

<read_first>
- `src/claw_cron/channels/email.py` — 当前状态（send_text 已实现）
</read_first>

<action>
扩展 `send_text` 支持附件参数（EMAIL-05）。修改签名并添加附件处理：

```python
async def send_text(
    self,
    recipient: str,
    content: str,
    attachments: list[str] | None = None,
) -> MessageResult:
    self._validate_config()
    recipients = [r.strip() for r in recipient.split(",")]

    if attachments:
        msg = MIMEMultipart()
        msg.attach(MIMEText(content, "plain", "utf-8"))
        for path in attachments:
            with open(path, "rb") as f:
                part = MIMEBase("application", "octet-stream")
                part.set_payload(f.read())
            encoders.encode_base64(part)
            part.add_header(
                "Content-Disposition",
                f"attachment; filename={Path(path).name}",
            )
            msg.attach(part)
    else:
        msg = MIMEText(content, "plain", "utf-8")

    msg["Subject"] = content[:50]
    msg["From"] = self.config.from_email
    msg["To"] = ", ".join(recipients)

    try:
        await aiosmtplib.send(
            msg,
            hostname=self.config.host,
            port=self.config.port,
            username=self.config.username,
            password=self.config.password,
            start_tls=self.config.use_tls,
        )
        return MessageResult(success=True)
    except aiosmtplib.SMTPAuthenticationError as e:
        raise ChannelAuthError(str(e), channel_id=self.channel_id)
    except aiosmtplib.SMTPException as e:
        return MessageResult(success=False, error=str(e))
```

导入：`from email.mime.base import MIMEBase`, `from email import encoders`, `from pathlib import Path`
</action>

<acceptance_criteria>
- `send_text` 签名包含 `attachments: list[str] | None = None`
- `email.py` 包含 `from email.mime.base import MIMEBase`
- `email.py` 包含 `from email import encoders`
- `email.py` 包含 `from pathlib import Path`
- 当 `attachments` 非空时，创建 `MIMEMultipart()` 并调用 `encoders.encode_base64`
- 当 `attachments` 为 None 时，仍使用 `MIMEText`（向后兼容）
</acceptance_criteria>

---

## Verification

```bash
# 文件存在
test -f src/claw_cron/channels/email.py && echo "OK"

# 类定义存在
grep -n "class EmailConfig" src/claw_cron/channels/email.py
grep -n "class EmailChannel" src/claw_cron/channels/email.py

# 关键方法存在
grep -n "async def send_text" src/claw_cron/channels/email.py
grep -n "async def send_markdown" src/claw_cron/channels/email.py
grep -n "def _validate_config" src/claw_cron/channels/email.py

# 依赖导入
grep -n "import aiosmtplib" src/claw_cron/channels/email.py
grep -n "import markdown" src/claw_cron/channels/email.py

# 语法检查
python -c "from claw_cron.channels.email import EmailChannel, EmailConfig; print('Import OK')"
```

## Must Haves

- [ ] `EmailConfig` 包含 D-06 中所有 6 个字段（host, port, username, password, from_email, use_tls）
- [ ] `send_text` 支持逗号分隔多收件人（D-04）
- [ ] `send_markdown` 使用 `markdown` 库转换为 HTML（D-03）
- [ ] `send_text` 支持 `attachments: list[str]` 文件路径附件（D-02）
- [ ] 所有方法返回 `MessageResult`
