---
plan: 13-02
phase: 13
wave: 2
title: CLI Integration for Email Channel
depends_on:
  - 13-01
files_modified:
  - src/claw_cron/channels/__init__.py
  - src/claw_cron/cmd/channels.py
autonomous: true
requirements_addressed:
  - EMAIL-01
---

# Plan 13-02: CLI Integration for Email Channel

## Objective

将 `EmailChannel` 注册到 `CHANNEL_REGISTRY`，并在 `channels add` 和 `channels verify` 命令中添加 email 通道支持，包括 SMTP 配置收集和测试邮件验证（D-05）。

## Context

<context>
- `src/claw_cron/channels/__init__.py` — CHANNEL_REGISTRY，get_channel_status（需扩展 email 分支）
- `src/claw_cron/cmd/channels.py` — add() 和 verify() 命令（feishu 分支为参考模式）
- `src/claw_cron/channels/email.py` — Plan 13-01 产出（EmailChannel, EmailConfig）
</context>

## Tasks

### Task 1: Register EmailChannel in CHANNEL_REGISTRY

<read_first>
- `src/claw_cron/channels/__init__.py` — 当前文件（查看 feishu/qqbot 注册模式）
</read_first>

<action>
在 `src/claw_cron/channels/__init__.py` 中：

1. 在 `__all__` 列表中添加 `"EmailChannel"`

2. 在文件末尾的注册区块添加：
```python
from .email import EmailChannel
CHANNEL_REGISTRY["email"] = EmailChannel
```

3. 在 `get_channel_status` 函数的 channel-specific validation 区块中添加 email 分支：
```python
elif channel_id == "email":
    required = ["host", "username", "password", "from_email"]
    if any(field not in channel_cfg for field in required):
        return "⚠", "配置不完整"
```
</action>

<acceptance_criteria>
- `__init__.py` 包含 `from .email import EmailChannel`
- `__init__.py` 包含 `CHANNEL_REGISTRY["email"] = EmailChannel`
- `__init__.py` 的 `get_channel_status` 包含 `elif channel_id == "email":`
- `python -c "from claw_cron.channels import EmailChannel, CHANNEL_REGISTRY; assert 'email' in CHANNEL_REGISTRY; print('OK')"` 输出 `OK`
</acceptance_criteria>

---

### Task 2: Add email branch to `channels add` command

<read_first>
- `src/claw_cron/cmd/channels.py` — 当前文件（查看 feishu 分支的完整实现模式）
</read_first>

<action>
在 `src/claw_cron/cmd/channels.py` 的 `add()` 函数中，在 `elif channel_type == "imessage":` 之前添加 email 分支：

```python
elif channel_type == "email":
    host = click.prompt("SMTP Host", type=str)
    port = click.prompt("SMTP Port", type=int, default=587)
    username = click.prompt("Username", type=str)
    password = click.prompt("Password", type=str, hide_input=True)
    from_email = click.prompt("From Email", type=str)
    use_tls = click.confirm("Use TLS", default=True)

    # Validate by sending a test email (D-05)
    with console.status("[bold green]正在发送验证邮件..."):
        try:
            import asyncio as _asyncio
            from claw_cron.channels.email import EmailChannel, EmailConfig
            test_config = EmailConfig(
                host=host, port=port, username=username,
                password=password, from_email=from_email, use_tls=use_tls,
            )
            channel = EmailChannel(test_config)
            result = _asyncio.run(channel.send_text(
                from_email,
                "claw-cron 邮件通道验证邮件\n\n此邮件由 claw-cron 自动发送，用于验证 SMTP 配置。",
            ))
            if not result.success:
                raise click.ClickException(f"验证失败: {result.error}")
            console.print("[green]✓ 验证邮件已发送[/green]")
        except Exception as e:
            if isinstance(e, click.ClickException):
                raise
            raise click.ClickException(f"连接失败: {e}") from e

    # Save to config.yaml
    if "channels" not in config:
        config["channels"] = {}
    config["channels"][channel_type] = {
        "host": host,
        "port": port,
        "username": username,
        "password": password,
        "from_email": from_email,
        "use_tls": use_tls,
        "enabled": True,
        "created_at": datetime.now().isoformat(),
    }
    save_config(config)
    console.print(f"[green]✓ 通道 '{channel_type}' 配置完成[/green]")
```
</action>

<acceptance_criteria>
- `channels.py` 包含 `elif channel_type == "email":`
- `channels.py` 包含 `SMTP Host` prompt
- `channels.py` 包含 `SMTP Port` prompt（默认 587）
- `channels.py` 包含 `hide_input=True` 的 Password prompt
- `channels.py` 包含 `Use TLS` confirm（默认 True）
- `channels.py` 包含发送测试邮件到 `from_email` 的验证逻辑
- `channels.py` 保存 `host`, `port`, `username`, `password`, `from_email`, `use_tls` 到 config
</acceptance_criteria>

---

### Task 3: Add email branch to `channels verify` command

<read_first>
- `src/claw_cron/cmd/channels.py` — 当前文件（查看 verify() 函数中 feishu 分支）
</read_first>

<action>
在 `verify()` 函数中：

1. 更新 `@click.argument("channel_type", type=click.Choice([...]))` 添加 `"email"`：
```python
@click.argument("channel_type", type=click.Choice(["qqbot", "feishu", "imessage", "email"]))
```

2. 在 `elif channel_type == "imessage":` 之前添加 email 分支：
```python
elif channel_type == "email":
    email_cfg = channels_config["email"]
    required_fields = ["host", "username", "password", "from_email"]
    missing = [f for f in required_fields if not email_cfg.get(f)]
    if missing:
        console.print(f"[red]✗ 配置不完整：缺少 {', '.join(missing)}[/red]")
        raise SystemExit(1)

    with console.status("[bold green]正在发送验证邮件..."):
        try:
            import asyncio as _asyncio
            from claw_cron.channels.email import EmailChannel, EmailConfig
            test_config = EmailConfig(**{k: v for k, v in email_cfg.items() if k != "created_at"})
            channel = EmailChannel(test_config)
            result = _asyncio.run(channel.send_text(
                email_cfg["from_email"],
                "claw-cron 邮件通道验证邮件",
            ))
            if not result.success:
                console.print(f"[red]✗ 验证失败: {result.error}[/red]")
                raise SystemExit(1)
            console.print("[green]✓ 验证邮件已发送[/green]")
            console.print(f"[dim]SMTP Host: {email_cfg['host']}:{email_cfg.get('port', 587)}[/dim]")
            console.print(f"[dim]From: {email_cfg['from_email']}[/dim]")
        except SystemExit:
            raise
        except Exception as e:
            console.print(f"[red]✗ 连接失败: {e}[/red]")
            raise SystemExit(1)
```
</action>

<acceptance_criteria>
- `verify()` 的 `click.Choice` 包含 `"email"`
- `channels.py` 包含 `elif channel_type == "email":` 在 verify 函数中
- verify email 分支检查 `host`, `username`, `password`, `from_email` 四个必填字段
- verify email 分支发送测试邮件并显示 SMTP host 和 from_email 信息
- `claw-cron channels verify --help` 输出包含 `email` 选项（通过 `grep "email"` 验证）
</acceptance_criteria>

---

## Verification

```bash
# 注册验证
python -c "
from claw_cron.channels import CHANNEL_REGISTRY, get_channel_status
assert 'email' in CHANNEL_REGISTRY, 'email not in registry'
icon, text = get_channel_status('email')
assert icon == '○', f'Expected ○ for unconfigured, got {icon}'
print('Registry OK')
"

# CLI 帮助验证
python -m claw_cron channels add --help | grep -i "email" || echo "email not in add help"
python -m claw_cron channels verify --help | grep "email"

# 语法检查
python -c "from claw_cron.cmd.channels import channels; print('CLI import OK')"
```

## Must Haves

- [ ] `CHANNEL_REGISTRY["email"]` 指向 `EmailChannel`
- [ ] `get_channel_status("email")` 正确检查 host/username/password/from_email 四个字段
- [ ] `channels add` 支持 email 通道，收集 D-06 中所有配置字段
- [ ] `channels add email` 发送测试邮件到 from_email 验证配置（D-05）
- [ ] `channels verify email` 重新验证已保存的 email 配置
