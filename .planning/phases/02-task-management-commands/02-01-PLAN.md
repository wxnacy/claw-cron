---
plan: "02-01"
phase: 2
wave: 1
title: "List & Delete Commands"
depends_on: []
requirements_addressed: ["LIST-01", "DELETE-01"]
files_modified:
  - src/claw_cron/cli.py
  - src/claw_cron/cmd/list.py
  - src/claw_cron/cmd/delete.py
autonomous: true
---

# Plan 01: List & Delete Commands

## Objective

实现 `claw-cron list` 和 `claw-cron delete <name>` 命令，并在 `cli.py` 中注册子命令。`list` 用 Rich Table 展示所有任务，`delete` 按名称删除并要求确认。

## Context

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-task-management-commands/02-RESEARCH.md
@.planning/phases/01-project-foundation/01-02-SUMMARY.md
</context>

## Tasks

### Task 1: 实现 list 命令

<task type="auto">
<name>implement-list</name>
<action>
写入 `src/claw_cron/cmd/list.py`，完整内容如下：

```python
# SPDX-FileCopyrightText: 2026-present wxnacy <371032668@qq.com>
#
# SPDX-License-Identifier: MIT

"""list command — show all scheduled tasks."""

import click
from rich.console import Console
from rich.table import Table

from claw_cron.storage import load_tasks

console = Console()


@click.command("list")
def list_tasks() -> None:
    """List all scheduled tasks."""
    tasks = load_tasks()
    if not tasks:
        console.print("[yellow]No tasks found.[/yellow]")
        return

    table = Table(title="Tasks", show_header=True, header_style="bold")
    table.add_column("Name", style="cyan")
    table.add_column("Cron", style="green")
    table.add_column("Type")
    table.add_column("Script/Prompt", overflow="fold", max_width=40)
    table.add_column("Status")

    for t in tasks:
        content = t.script or t.prompt or "-"
        status = "✓ enabled" if t.enabled else "✗ disabled"
        table.add_row(t.name, t.cron, t.type, content, status)

    console.print(table)
```
</action>
<read_first>
- src/claw_cron/cmd/list.py（如已存在）
- src/claw_cron/storage.py（确认 load_tasks 签名和 Task 字段）
</read_first>
<acceptance_criteria>
- `src/claw_cron/cmd/list.py` 包含 `@click.command("list")`
- `src/claw_cron/cmd/list.py` 包含 `from claw_cron.storage import load_tasks`
- `src/claw_cron/cmd/list.py` 包含 `Table(title="Tasks"`
- `src/claw_cron/cmd/list.py` 包含 `"No tasks found."` 空列表处理
</acceptance_criteria>
</task>

### Task 2: 实现 delete 命令

<task type="auto">
<name>implement-delete</name>
<action>
写入 `src/claw_cron/cmd/delete.py`，完整内容如下：

```python
# SPDX-FileCopyrightText: 2026-present wxnacy <371032668@qq.com>
#
# SPDX-License-Identifier: MIT

"""delete command — remove a scheduled task by name."""

import click
from rich.console import Console

from claw_cron.storage import delete_task, get_task

console = Console()


@click.command()
@click.argument("name")
def delete(name: str) -> None:
    """Delete a scheduled task by NAME."""
    task = get_task(name)
    if task is None:
        console.print(f"[red]Task '{name}' not found.[/red]")
        raise SystemExit(1)

    click.confirm(f"Delete task '{name}'?", abort=True)
    delete_task(name)
    console.print(f"[green]Task '{name}' deleted.[/green]")
```
</action>
<read_first>
- src/claw_cron/cmd/delete.py（如已存在）
- src/claw_cron/storage.py（确认 delete_task 和 get_task 签名）
</read_first>
<acceptance_criteria>
- `src/claw_cron/cmd/delete.py` 包含 `@click.argument("name")`
- `src/claw_cron/cmd/delete.py` 包含 `from claw_cron.storage import delete_task, get_task`
- `src/claw_cron/cmd/delete.py` 包含 `click.confirm(` 确认步骤
- `src/claw_cron/cmd/delete.py` 包含 `"not found"` 错误处理
- `src/claw_cron/cmd/delete.py` 包含 `raise SystemExit(1)` 任务不存在时退出
</acceptance_criteria>
</task>

### Task 3: 在 cli.py 注册子命令

<task type="auto">
<name>register-commands</name>
<action>
修改 `src/claw_cron/cli.py`，注册 list 和 delete 子命令：

```python
# SPDX-FileCopyrightText: 2026-present wxnacy <371032668@qq.com>
#
# SPDX-License-Identifier: MIT

import click

from claw_cron.__about__ import __version__
from claw_cron.cmd.delete import delete
from claw_cron.cmd.list import list_tasks


@click.group(context_settings={"help_option_names": ["-h", "--help"]})
@click.version_option(__version__, "-v", "--version")
def cli() -> None:
    """claw-cron: AI-powered cron task manager."""
    pass


cli.add_command(list_tasks)
cli.add_command(delete)
```
</action>
<read_first>
- src/claw_cron/cli.py（当前内容，确认现有结构）
- src/claw_cron/cmd/list.py（确认导出名称 list_tasks）
- src/claw_cron/cmd/delete.py（确认导出名称 delete）
</read_first>
<acceptance_criteria>
- `src/claw_cron/cli.py` 包含 `from claw_cron.cmd.delete import delete`
- `src/claw_cron/cli.py` 包含 `from claw_cron.cmd.list import list_tasks`
- `src/claw_cron/cli.py` 包含 `cli.add_command(list_tasks)`
- `src/claw_cron/cli.py` 包含 `cli.add_command(delete)`
</acceptance_criteria>
</task>

### Task 4: 验证命令可运行

<task type="auto">
<name>verify-commands</name>
<action>
运行以下命令验证：

```bash
# 验证 list 命令（空任务列表）
uv run claw-cron list

# 验证 delete 命令帮助
uv run claw-cron delete --help

# 验证 -h 仍然显示新子命令
uv run claw-cron -h
```
</action>
<read_first>
- src/claw_cron/cli.py
</read_first>
<acceptance_criteria>
- `uv run claw-cron list` 退出码为 0，输出包含 `No tasks found` 或任务表格
- `uv run claw-cron delete --help` 退出码为 0，输出包含 `NAME`
- `uv run claw-cron -h` 输出包含 `list` 和 `delete` 子命令
</acceptance_criteria>
</task>

## Verification

```bash
uv run claw-cron -h          # 显示 list, delete 子命令
uv run claw-cron list        # 空列表或任务表格
uv run claw-cron delete -h   # 显示 NAME 参数
```

## Must Haves

- [ ] `claw-cron list` 以 Rich 表格展示任务（空时显示提示）
- [ ] `claw-cron delete <name>` 任务不存在时退出码非 0
- [ ] `claw-cron delete <name>` 删除前要求确认
- [ ] `claw-cron -h` 显示 list 和 delete 子命令
