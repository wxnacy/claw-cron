---
plan: "02-02"
phase: 2
wave: 2
title: "Add Command — Direct Mode"
depends_on: ["02-01"]
requirements_addressed: ["ADD-02", "ADD-03"]
files_modified:
  - src/claw_cron/cmd/add.py
  - src/claw_cron/cli.py
autonomous: true
---

# Plan 02: Add Command — Direct Mode

## Objective

实现 `claw-cron add` 命令的直接模式：当 `--name`、`--cron`、`--type` 均提供时，跳过 AI 交互直接写入任务。支持 `--script`（command 类型）和 `--prompt`/`--client`（agent 类型）。供其他 Agent 作为 skill 调用（ADD-03）。

## Context

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-task-management-commands/02-RESEARCH.md
@.planning/phases/02-task-management-commands/02-01-PLAN.md
</context>

## Tasks

### Task 1: 实现 add 命令（直接模式框架）

<task type="auto">
<name>implement-add-direct</name>
<action>
写入 `src/claw_cron/cmd/add.py`，完整内容如下：

```python
# SPDX-FileCopyrightText: 2026-present wxnacy <371032668@qq.com>
#
# SPDX-License-Identifier: MIT

"""add command — create a new scheduled task."""

import click
from rich.console import Console

from claw_cron.storage import Task, add_task

console = Console()


@click.command()
@click.option("--name", default=None, help="Unique task name")
@click.option("--cron", default=None, help="Cron expression (5 fields, e.g. '0 8 * * *')")
@click.option(
    "--type",
    "task_type",
    default=None,
    type=click.Choice(["command", "agent"]),
    help="Execution type",
)
@click.option("--script", default=None, help="Shell command to run (command type)")
@click.option("--prompt", "ai_prompt", default=None, help="AI prompt to send (agent type)")
@click.option(
    "--client",
    default=None,
    type=click.Choice(["kiro-cli", "codebuddy", "opencode"]),
    help="AI client to use (agent type)",
)
def add(
    name: str | None,
    cron: str | None,
    task_type: str | None,
    script: str | None,
    ai_prompt: str | None,
    client: str | None,
) -> None:
    """Add a new scheduled task.

    Direct mode: provide --name, --cron, --type (and --script or --prompt) to skip AI interaction.
    Interactive mode: omit required options to start an AI-guided conversation.
    """
    # Direct mode: all required fields provided
    if name and cron and task_type:
        _add_direct(name, cron, task_type, script, ai_prompt, client)
        return

    # AI interactive mode (Phase 2 Plan 03)
    from claw_cron.agent import run_ai_add  # noqa: PLC0415

    run_ai_add(name=name, cron=cron, task_type=task_type, script=script, ai_prompt=ai_prompt, client=client)


def _add_direct(
    name: str,
    cron: str,
    task_type: str,
    script: str | None,
    ai_prompt: str | None,
    client: str | None,
) -> None:
    """Create task directly from provided arguments."""
    if task_type == "command" and not script:
        raise click.UsageError("--script is required for command type tasks")
    if task_type == "agent" and not ai_prompt:
        raise click.UsageError("--prompt is required for agent type tasks")

    task = Task(
        name=name,
        cron=cron,
        type=task_type,
        script=script,
        prompt=ai_prompt,
        client=client,
    )
    add_task(task)
    console.print(f"[green]Task '{name}' added.[/green]")
```
</action>
<read_first>
- src/claw_cron/cmd/add.py（如已存在）
- src/claw_cron/storage.py（确认 Task dataclass 字段和 add_task 签名）
</read_first>
<acceptance_criteria>
- `src/claw_cron/cmd/add.py` 包含 `@click.option("--name"`
- `src/claw_cron/cmd/add.py` 包含 `@click.option("--cron"`
- `src/claw_cron/cmd/add.py` 包含 `type=click.Choice(["command", "agent"])`
- `src/claw_cron/cmd/add.py` 包含 `if name and cron and task_type:` 直接模式判断
- `src/claw_cron/cmd/add.py` 包含 `def _add_direct(` 函数
- `src/claw_cron/cmd/add.py` 包含 `raise click.UsageError` 参数校验
- `src/claw_cron/cmd/add.py` 包含 `from claw_cron.agent import run_ai_add` AI 模式入口
</acceptance_criteria>
</task>

### Task 2: 在 cli.py 注册 add 命令

<task type="auto">
<name>register-add-command</name>
<action>
修改 `src/claw_cron/cli.py`，添加 add 命令注册：

```python
# SPDX-FileCopyrightText: 2026-present wxnacy <371032668@qq.com>
#
# SPDX-License-Identifier: MIT

import click

from claw_cron.__about__ import __version__
from claw_cron.cmd.add import add
from claw_cron.cmd.delete import delete
from claw_cron.cmd.list import list_tasks


@click.group(context_settings={"help_option_names": ["-h", "--help"]})
@click.version_option(__version__, "-v", "--version")
def cli() -> None:
    """claw-cron: AI-powered cron task manager."""
    pass


cli.add_command(add)
cli.add_command(list_tasks)
cli.add_command(delete)
```
</action>
<read_first>
- src/claw_cron/cli.py（当前内容，确认已有 list 和 delete 注册）
</read_first>
<acceptance_criteria>
- `src/claw_cron/cli.py` 包含 `from claw_cron.cmd.add import add`
- `src/claw_cron/cli.py` 包含 `cli.add_command(add)`
- `src/claw_cron/cli.py` 仍包含 `cli.add_command(list_tasks)` 和 `cli.add_command(delete)`
</acceptance_criteria>
</task>

### Task 3: 创建 agent.py 占位（供 Plan 03 填充）

<task type="auto">
<name>create-agent-placeholder</name>
<action>
写入 `src/claw_cron/agent.py`，内容为占位实现（Plan 03 会替换）：

```python
# SPDX-FileCopyrightText: 2026-present wxnacy <371032668@qq.com>
#
# SPDX-License-Identifier: MIT

"""Anthropic Agent for interactive task creation (ADD-01, ADD-04).

Full implementation in Phase 2 Plan 03.
"""

from __future__ import annotations


def run_ai_add(
    name: str | None = None,
    cron: str | None = None,
    task_type: str | None = None,
    script: str | None = None,
    ai_prompt: str | None = None,
    client: str | None = None,
) -> None:
    """Run AI-guided task creation (not yet implemented)."""
    raise NotImplementedError("AI interactive mode will be implemented in Plan 03")
```
</action>
<read_first>
- src/claw_cron/agent.py（如已存在）
</read_first>
<acceptance_criteria>
- `src/claw_cron/agent.py` 文件存在
- `src/claw_cron/agent.py` 包含 `def run_ai_add(`
- `src/claw_cron/agent.py` 包含 `raise NotImplementedError`
</acceptance_criteria>
</task>

### Task 4: 验证直接模式

<task type="auto">
<name>verify-direct-mode</name>
<action>
运行以下命令验证直接模式正常工作：

```bash
# 添加一个 command 类型任务
uv run claw-cron add --name test-task --cron "0 8 * * *" --type command --script "echo hello"

# 验证任务已写入
uv run claw-cron list

# 清理测试数据
uv run claw-cron delete test-task

# 验证参数校验：command 类型缺少 --script
uv run claw-cron add --name bad --cron "0 8 * * *" --type command 2>&1 | grep -i "script"

# 验证 add -h 显示所有选项
uv run claw-cron add -h
```
</action>
<read_first>
- src/claw_cron/cmd/add.py
- src/claw_cron/storage.py
</read_first>
<acceptance_criteria>
- `uv run claw-cron add --name test-task --cron "0 8 * * *" --type command --script "echo hello"` 退出码为 0
- `uv run claw-cron list` 输出包含 `test-task`
- `uv run claw-cron delete test-task` 退出码为 0（确认后）
- `uv run claw-cron add --name bad --cron "0 8 * * *" --type command` 输出包含 `script`（错误提示）
- `uv run claw-cron add -h` 输出包含 `--name`, `--cron`, `--type`, `--script`, `--prompt`, `--client`
</acceptance_criteria>
</task>

## Verification

```bash
uv run claw-cron add --name t --cron "0 8 * * *" --type command --script "echo hi"
uv run claw-cron list   # 显示 t
uv run claw-cron delete t
uv run claw-cron -h     # 显示 add, list, delete
```

## Must Haves

- [ ] 直接模式：`--name` + `--cron` + `--type` + `--script` 全提供时直接写入，退出码 0
- [ ] command 类型缺少 `--script` 时报错退出
- [ ] agent 类型缺少 `--prompt` 时报错退出
- [ ] `claw-cron -h` 显示 add 子命令
