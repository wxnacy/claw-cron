---
plan: 18-01
phase: 18
title: Data Model & Context Storage
wave: 1
depends_on: []
files_modified:
  - src/claw_cron/storage.py
  - src/claw_cron/notifier.py
  - src/claw_cron/context.py
autonomous: true
requirements_addressed:
  - CTX-02
  - CTX-06
  - COND-01
---

# Plan 18-01: Data Model & Context Storage

## Objective

扩展 Task 和 NotifyConfig 数据模型，新增 `env` 和 `when` 字段；创建独立的 `context.py` 模块实现任务上下文的 JSON 持久化。

## must_haves

- [ ] `Task.env` 字段存在，类型为 `dict[str, str] | None`，默认 `None`
- [ ] `NotifyConfig.when` 字段存在，类型为 `str | None`，默认 `None`
- [ ] `context.py` 模块存在，提供 `load_context` / `save_context` 函数
- [ ] 现有无 `env`/`when` 字段的 YAML 任务可正常加载（向后兼容）
- [ ] context 文件路径为 `~/.config/claw-cron/context/{task_name}.json`

---

## Wave 1 Tasks

### Task 1: 扩展 Task dataclass — 添加 env 字段

<read_first>
- `src/claw_cron/storage.py` — Task dataclass 定义（line 22-47），`_task_from_dict` 反序列化逻辑
</read_first>

<action>
在 `src/claw_cron/storage.py` 中：

1. 在 `Task` dataclass 末尾添加字段（在 `message` 字段之后）：
   ```python
   env: dict[str, str] | None = None
   ```

2. 更新 docstring Attributes 部分，添加：
   ```
   env: Custom environment variables to inject with CLAW_CONTEXT_ prefix. Optional.
   ```

3. 在 `_task_from_dict` 函数中，`env` 字段无需特殊处理——YAML 中的 dict 直接映射到 `dict[str, str]`，但需确保 `Task(**raw)` 不会因旧 YAML 缺少 `env` 字段而报错。由于 `env` 有默认值 `None`，`Task(**raw)` 天然向后兼容，无需额外处理。

注意：`asdict()` 序列化时 `env: None` 会写入 YAML 为 `env: null`。这是可接受的行为。
</action>

<acceptance_criteria>
- `grep "env: dict\[str, str\] | None" src/claw_cron/storage.py` 输出非空
- `grep "env: None" src/claw_cron/storage.py` 输出非空（默认值）
- Python 执行 `from claw_cron.storage import Task; t = Task(name='x', cron='* * * * *', type='command'); assert t.env is None` 无异常
- Python 执行 `from claw_cron.storage import _task_from_dict; t = _task_from_dict({'name':'x','cron':'* * * * *','type':'command'}); assert t.env is None` 无异常（向后兼容）
</acceptance_criteria>

---

### Task 2: 扩展 NotifyConfig dataclass — 添加 when 字段

<read_first>
- `src/claw_cron/notifier.py` — NotifyConfig dataclass 定义（line 42-83），`from_dict` 方法
</read_first>

<action>
在 `src/claw_cron/notifier.py` 中：

1. 在 `NotifyConfig` dataclass 的 `recipients` 字段之后添加：
   ```python
   when: str | None = None
   ```

2. 更新 docstring Attributes 部分，添加：
   ```
   when: Condition expression for notification delivery (e.g. 'signed_in == false').
       When None, notification is always sent. Evaluated in Phase 20.
   ```

3. 在 `from_dict` 方法中添加 `when` 字段读取：
   ```python
   return cls(
       channel=data.get("channel", ""),
       recipients=data.get("recipients", []),
       when=data.get("when"),
   )
   ```

向后兼容：`data.get("when")` 在旧 YAML 无 `when` 字段时返回 `None`，天然兼容。
</action>

<acceptance_criteria>
- `grep "when: str | None" src/claw_cron/notifier.py` 输出非空
- `grep "when=data.get" src/claw_cron/notifier.py` 输出非空
- Python 执行 `from claw_cron.notifier import NotifyConfig; n = NotifyConfig.from_dict({'channel':'imessage','recipients':[]}); assert n.when is None` 无异常
- Python 执行 `from claw_cron.notifier import NotifyConfig; n = NotifyConfig.from_dict({'channel':'imessage','recipients':[],'when':'signed_in == false'}); assert n.when == 'signed_in == false'` 无异常
</acceptance_criteria>

---

### Task 3: 新建 context.py 模块

<read_first>
- `src/claw_cron/storage.py` — `_load_raw` / `save_tasks` 的文件读写模式参考
- `src/claw_cron/executor.py` — `LOGS_DIR` 路径定义模式参考（`Path.home() / ".config" / "claw-cron" / ...`）
</read_first>

<action>
创建 `src/claw_cron/context.py`，内容如下：

```python
# SPDX-FileCopyrightText: 2026-present wxnacy <371032668@qq.com>
#
# SPDX-License-Identifier: MIT

"""Task context persistence — load and save per-task context JSON files."""

from __future__ import annotations

import json
from pathlib import Path
from typing import Any

CONTEXT_DIR = Path.home() / ".config" / "claw-cron" / "context"


def _context_path(task_name: str) -> Path:
    """Return the context file path for a task."""
    return CONTEXT_DIR / f"{task_name}.json"


def load_context(task_name: str) -> dict[str, Any]:
    """Load task context from JSON file.

    Args:
        task_name: Task name used as the context file key.

    Returns:
        Context dict. Empty dict if file doesn't exist or is invalid JSON.
    """
    path = _context_path(task_name)
    if not path.exists():
        return {}
    try:
        return json.loads(path.read_text())
    except (json.JSONDecodeError, OSError):
        return {}


def save_context(task_name: str, context: dict[str, Any]) -> None:
    """Save task context to JSON file.

    Creates parent directories if they don't exist.

    Args:
        task_name: Task name used as the context file key.
        context: Context dict to persist.
    """
    path = _context_path(task_name)
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(json.dumps(context, ensure_ascii=False, indent=2))
```
</action>

<acceptance_criteria>
- `test -f src/claw_cron/context.py` 返回 0
- `grep "CONTEXT_DIR = Path.home" src/claw_cron/context.py` 输出非空
- `grep "claw-cron/context" src/claw_cron/context.py` 输出非空
- `grep "def load_context" src/claw_cron/context.py` 输出非空
- `grep "def save_context" src/claw_cron/context.py` 输出非空
- Python 执行以下代码无异常：
  ```python
  from claw_cron.context import load_context, save_context
  save_context("test_task", {"signed_in": "true", "last_exit_code": "0"})
  ctx = load_context("test_task")
  assert ctx["signed_in"] == "true"
  assert ctx["last_exit_code"] == "0"
  ```
- Python 执行 `from claw_cron.context import load_context; assert load_context("nonexistent_task_xyz") == {}` 无异常（文件不存在时返回空 dict）
</acceptance_criteria>

---

## Verification

```bash
# 1. 检查所有文件存在
test -f src/claw_cron/storage.py && echo "storage.py OK"
test -f src/claw_cron/notifier.py && echo "notifier.py OK"
test -f src/claw_cron/context.py && echo "context.py OK"

# 2. 检查字段定义
grep "env: dict\[str, str\] | None" src/claw_cron/storage.py
grep "when: str | None" src/claw_cron/notifier.py
grep "def load_context\|def save_context" src/claw_cron/context.py

# 3. 向后兼容验证
python -c "
from claw_cron.storage import _task_from_dict
from claw_cron.notifier import NotifyConfig

# 旧格式 Task（无 env 字段）
t = _task_from_dict({'name': 'old_task', 'cron': '* * * * *', 'type': 'command', 'script': 'echo hi'})
assert t.env is None, 'env should default to None'

# 旧格式 NotifyConfig（无 when 字段）
n = NotifyConfig.from_dict({'channel': 'imessage', 'recipients': ['+86138']})
assert n.when is None, 'when should default to None'

print('Backward compatibility: OK')
"

# 4. context.py 读写验证
python -c "
from claw_cron.context import load_context, save_context
save_context('__test__', {'key': 'value'})
ctx = load_context('__test__')
assert ctx == {'key': 'value'}
# 清理
import os; os.remove(os.path.expanduser('~/.config/claw-cron/context/__test__.json'))
print('Context read/write: OK')
"

# 5. 运行现有测试（如有）
python -m pytest tests/ -x -q 2>/dev/null || echo "No tests or tests passed"
```
