---
plan: "01-02"
phase: 1
wave: 2
title: "YAML Storage Layer"
depends_on: ["01-01"]
requirements_addressed: ["STORE-01", "STORE-02"]
files_modified:
  - src/claw_cron/storage.py
autonomous: true
---

# Plan 02: YAML Storage Layer

## Objective

实现任务配置的 YAML 存储层：定义 `Task` dataclass（包含所有必要字段），实现 `~/.config/claw-cron/tasks.yaml` 的读写操作，提供 `load_tasks`、`save_tasks`、`add_task`、`get_task`、`delete_task` 函数。

## Context

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@.planning/phases/01-project-foundation/01-01-PLAN.md
</context>

## Tasks

### Task 1: 实现 storage.py

<task type="auto">
<name>implement-storage</name>
<action>
写入 `src/claw_cron/storage.py`，完整内容如下：

```python
# SPDX-FileCopyrightText: 2026-present wxnacy <371032668@qq.com>
#
# SPDX-License-Identifier: MIT

"""YAML-based task storage for claw-cron."""

from __future__ import annotations

from dataclasses import asdict, dataclass, field
from pathlib import Path
from typing import Any

import yaml

TASKS_FILE = Path.home() / ".config" / "claw-cron" / "tasks.yaml"


@dataclass
class Task:
    """A scheduled task configuration.

    Attributes:
        name: Unique task identifier.
        cron: Standard 5-field cron expression (min hour day month weekday).
        type: Execution type — "command" or "agent".
        script: Shell command to run (command type).
        prompt: AI prompt to send (agent type).
        client: AI client to use — "kiro-cli", "codebuddy", or "opencode" (agent type).
        enabled: Whether the task is active. Defaults to True.
    """

    name: str
    cron: str
    type: str
    script: str | None = None
    prompt: str | None = None
    client: str | None = None
    enabled: bool = field(default=True)


def _load_raw(path: Path = TASKS_FILE) -> list[dict[str, Any]]:
    """Load raw task dicts from YAML file."""
    if not path.exists():
        return []
    with path.open() as f:
        data = yaml.safe_load(f) or {}
    return data.get("tasks", [])


def load_tasks(path: Path = TASKS_FILE) -> list[Task]:
    """Load all tasks from the YAML file.

    Args:
        path: Path to the tasks YAML file.

    Returns:
        List of Task objects. Empty list if file doesn't exist.
    """
    return [Task(**raw) for raw in _load_raw(path)]


def save_tasks(tasks: list[Task], path: Path = TASKS_FILE) -> None:
    """Save tasks to the YAML file.

    Creates parent directories if they don't exist.

    Args:
        tasks: List of Task objects to persist.
        path: Path to the tasks YAML file.
    """
    path.parent.mkdir(parents=True, exist_ok=True)
    with path.open("w") as f:
        yaml.dump({"tasks": [asdict(t) for t in tasks]}, f, default_flow_style=False, allow_unicode=True)


def get_task(name: str, path: Path = TASKS_FILE) -> Task | None:
    """Find a task by name.

    Args:
        name: Task name to search for.
        path: Path to the tasks YAML file.

    Returns:
        Task if found, None otherwise.
    """
    return next((t for t in load_tasks(path) if t.name == name), None)


def add_task(task: Task, path: Path = TASKS_FILE) -> None:
    """Add a new task, replacing any existing task with the same name.

    Args:
        task: Task to add or update.
        path: Path to the tasks YAML file.
    """
    tasks = [t for t in load_tasks(path) if t.name != task.name]
    tasks.append(task)
    save_tasks(tasks, path)


def delete_task(name: str, path: Path = TASKS_FILE) -> bool:
    """Delete a task by name.

    Args:
        name: Name of the task to delete.
        path: Path to the tasks YAML file.

    Returns:
        True if task was found and deleted, False if not found.
    """
    tasks = load_tasks(path)
    filtered = [t for t in tasks if t.name != name]
    if len(filtered) == len(tasks):
        return False
    save_tasks(filtered, path)
    return True
```
</action>
<read_first>
- src/claw_cron/storage.py（如已存在）
- .planning/phases/01-project-foundation/01-RESEARCH.md（Task 字段定义和 YAML 结构）
</read_first>
<acceptance_criteria>
- `src/claw_cron/storage.py` 包含 `@dataclass` 和 `class Task:`
- `src/claw_cron/storage.py` 包含字段 `name`, `cron`, `type`, `script`, `prompt`, `client`, `enabled`
- `src/claw_cron/storage.py` 包含 `TASKS_FILE = Path.home() / ".config" / "claw-cron" / "tasks.yaml"`
- `src/claw_cron/storage.py` 包含函数 `load_tasks`
- `src/claw_cron/storage.py` 包含函数 `save_tasks`
- `src/claw_cron/storage.py` 包含函数 `get_task`
- `src/claw_cron/storage.py` 包含函数 `add_task`
- `src/claw_cron/storage.py` 包含函数 `delete_task`
- `src/claw_cron/storage.py` 包含 `path.parent.mkdir(parents=True, exist_ok=True)` 在 `save_tasks`
</acceptance_criteria>
</task>

### Task 2: 验证 storage 模块可导入和基本功能

<task type="auto">
<name>verify-storage</name>
<action>
运行以下 Python 代码验证 storage 模块正常工作（使用临时文件，不污染生产数据）：

```bash
uv run python -c "
import tempfile
from pathlib import Path
from claw_cron.storage import Task, add_task, load_tasks, get_task, delete_task

# 使用临时文件测试
tmp = Path(tempfile.mktemp(suffix='.yaml'))

# 测试 add + load
t = Task(name='test', cron='0 8 * * *', type='command', script='echo hello')
add_task(t, tmp)
tasks = load_tasks(tmp)
assert len(tasks) == 1, f'Expected 1 task, got {len(tasks)}'
assert tasks[0].name == 'test'
assert tasks[0].enabled is True

# 测试 get_task
found = get_task('test', tmp)
assert found is not None
assert found.cron == '0 8 * * *'

# 测试 delete_task
result = delete_task('test', tmp)
assert result is True
assert len(load_tasks(tmp)) == 0

# 测试 delete 不存在的任务
result = delete_task('nonexistent', tmp)
assert result is False

tmp.unlink(missing_ok=True)
print('storage module: ALL TESTS PASSED')
"
```
</action>
<read_first>
- src/claw_cron/storage.py
</read_first>
<acceptance_criteria>
- 命令输出包含 `storage module: ALL TESTS PASSED`
- 命令退出码为 0
</acceptance_criteria>
</task>

## Verification

```bash
# 导入验证
uv run python -c "from claw_cron.storage import Task, load_tasks, save_tasks, add_task, get_task, delete_task; print('imports OK')"

# 字段验证
grep -n "name\|cron\|type\|script\|prompt\|client\|enabled" src/claw_cron/storage.py | grep "str\|bool"
```

## Must Haves

- [ ] `Task` dataclass 包含 STORE-02 要求的所有字段（name, cron, type, script, prompt, client, enabled）
- [ ] `save_tasks` 自动创建 `~/.config/claw-cron/` 目录
- [ ] `load_tasks` 在文件不存在时返回空列表（不抛异常）
- [ ] `delete_task` 返回 bool 表示是否找到并删除
