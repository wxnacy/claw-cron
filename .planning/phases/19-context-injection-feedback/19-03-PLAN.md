---
plan: "19-03"
phase: 19
title: "Add context CLI command — get and set task context"
wave: 2
depends_on:
  - "19-01"
files_modified:
  - src/claw_cron/cmd/context.py
  - src/claw_cron/cli.py
requirements_addressed:
  - CTX-01
  - CTX-03
  - CTX-04
  - CTX-05
autonomous: true
---

# Plan 19-03: Add context CLI command — get and set task context

## Objective

新增 `claw-cron context` 子命令组，支持 `get` 和 `set` 操作，供 AI agent 类型任务通过 subprocess 调用读写上下文。

## Tasks

### Task 1: Create src/claw_cron/cmd/context.py

<read_first>
- `src/claw_cron/cmd/command.py` — 现有子命令模块结构参考（Click group + subcommands 模式）
- `src/claw_cron/context.py` — `load_context` / `save_context` 函数签名
</read_first>

<action>
新建 `src/claw_cron/cmd/context.py`，内容如下：

```python
# SPDX-FileCopyrightText: 2026-present wxnacy <371032668@qq.com>
#
# SPDX-License-Identifier: MIT

"""Context CLI commands — get and set task context for AI agent use."""

from __future__ import annotations

import json

import click

from claw_cron.context import load_context, save_context


@click.group()
def context() -> None:
    """Manage task context (for AI agent use)."""


@context.command("get")
@click.argument("task_name")
def context_get(task_name: str) -> None:
    """Print task context as JSON.

    Outputs the full context JSON for TASK_NAME to stdout.
    AI agents can call this via subprocess to read task state.

    Example:
        claw-cron context get my_task
    """
    ctx = load_context(task_name)
    click.echo(json.dumps(ctx, ensure_ascii=False, indent=2))


@context.command("set")
@click.argument("task_name")
@click.option("--key", required=True, help="Context key to set.")
@click.option("--value", required=True, help="Value to set (string).")
def context_set(task_name: str, key: str, value: str) -> None:
    """Set a key-value pair in task context.

    AI agents can call this via subprocess to write task state.

    Example:
        claw-cron context set my_task --key signed_in --value true
    """
    ctx = load_context(task_name)
    ctx[key] = value
    save_context(task_name, ctx)
    click.echo(f"Set {key}={value} for task '{task_name}'")
```
</action>

<acceptance_criteria>
- `src/claw_cron/cmd/context.py` 文件存在
- 文件包含 `@click.group()` 和 `def context()`
- 文件包含 `@context.command("get")` 和 `def context_get`
- 文件包含 `@context.command("set")` 和 `def context_set`
- 文件包含 `--key` 和 `--value` 选项定义
- `python -c "from claw_cron.cmd.context import context; print('OK')"` 执行成功
</acceptance_criteria>

---

### Task 2: Register context command in cli.py

<read_first>
- `src/claw_cron/cli.py` — 现有 imports 和 `cli.add_command()` 调用列表
</read_first>

<action>
修改 `src/claw_cron/cli.py`：

1. 在现有 imports 中添加（按字母顺序插入到 `from claw_cron.cmd.delete import delete` 之前）：
   ```python
   from claw_cron.cmd.context import context
   ```

2. 在 `cli.add_command(channels)` 之后添加：
   ```python
   cli.add_command(context)
   ```
</action>

<acceptance_criteria>
- `cli.py` 包含 `from claw_cron.cmd.context import context`
- `cli.py` 包含 `cli.add_command(context)`
- `claw-cron context --help` 命令执行成功（exit code 0）
- `claw-cron context get --help` 命令执行成功
- `claw-cron context set --help` 命令执行成功
</acceptance_criteria>

## Verification

```bash
cd /Users/wxnacy/Projects/claw-cron
# Test context get on non-existent task returns empty JSON
claw-cron context get nonexistent_task_xyz
# Should output: {}

# Test context set and get roundtrip
claw-cron context set test_ctx_cmd --key signed_in --value true
claw-cron context get test_ctx_cmd
# Should output JSON containing "signed_in": "true"
```

```bash
cd /Users/wxnacy/Projects/claw-cron
python -c "
import subprocess, json

# set
r = subprocess.run(['claw-cron', 'context', 'set', 'test_ctx_cmd', '--key', 'foo', '--value', 'bar'],
                   capture_output=True, text=True)
assert r.returncode == 0, r.stderr

# get
r = subprocess.run(['claw-cron', 'context', 'get', 'test_ctx_cmd'],
                   capture_output=True, text=True)
assert r.returncode == 0, r.stderr
data = json.loads(r.stdout)
assert data.get('foo') == 'bar', f'Expected bar, got {data}'
print('context get/set: OK')
"
```

## must_haves

- `claw-cron context get <task_name>` 输出任务上下文 JSON
- `claw-cron context set <task_name> --key <k> --value <v>` 持久化键值对到 context.json
- 命令注册到 CLI 主入口，`claw-cron context --help` 可用
