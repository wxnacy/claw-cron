---
plan: "19-02"
phase: 19
title: "Extend executor.py — context injection and JSON stdout feedback"
wave: 2
depends_on:
  - "19-01"
files_modified:
  - src/claw_cron/executor.py
requirements_addressed:
  - CTX-01
  - CTX-04
  - CTX-05
autonomous: true
---

# Plan 19-02: Extend executor.py — context injection and JSON stdout feedback

## Objective

修改 `executor.py:execute_task()` 实现三件事：
1. 构建并注入系统 CLAW_ 环境变量（CTX-01）
2. 在执行前将上下文 JSON 写入固定路径文件，通过 CLAW_CONTEXT_FILE 传递（CTX-04）
3. 解析 stdout 最后一行 JSON，合并保存到 context.json（CTX-05）

同时修改 `execute_task_with_notify` 在执行后保存解析的上下文。

## Tasks

### Task 1: Add _build_env() helper to executor.py

<read_first>
- `src/claw_cron/executor.py` — 完整文件，了解现有 imports 和 execute_task 结构
- `src/claw_cron/context.py` — `load_context` / `save_context` 函数签名
- `src/claw_cron/storage.py` — Task dataclass，确认 `env` 字段类型为 `dict[str, str] | None`
</read_first>

<action>
在 `executor.py` 中添加以下 imports（在现有 imports 后）：

```python
import json
import os
from claw_cron.context import load_context, save_context
```

在 `_write_log` 函数后、`execute_task` 函数前，添加两个 helper 函数：

```python
CONTEXT_INPUT_DIR = Path.home() / ".config" / "claw-cron" / "context"


def _build_env(task: Task, context: dict) -> dict[str, str]:
    """Build environment variables dict for subprocess.

    Merges: current process env + system CLAW_ vars + user CLAW_CONTEXT_ vars.
    User env vars (CLAW_CONTEXT_ prefix) override system CLAW_ vars on name collision.

    Args:
        task: Task being executed.
        context: Current task context dict (loaded from context.json).

    Returns:
        Full environment dict for subprocess.
    """
    last_exit_code = str(context.get("last_exit_code", ""))
    last_output = context.get("last_output", "")
    if isinstance(last_output, str) and len(last_output) > 4096:
        last_output = last_output[:4096]

    system_vars: dict[str, str] = {
        "CLAW_TASK_NAME": task.name,
        "CLAW_TASK_TYPE": task.type,
        "CLAW_LAST_EXIT_CODE": last_exit_code,
        "CLAW_LAST_OUTPUT": str(last_output),
        "CLAW_EXECUTION_TIME": datetime.now().strftime("%Y-%m-%d %H:%M:%S"),
        "CLAW_TASK_CRON": task.cron,
        "CLAW_EXECUTION_COUNT": str(context.get("execution_count", 0)),
        "CLAW_LAST_EXECUTION_TIME": str(context.get("last_execution_time", "")),
    }

    # User env vars with CLAW_CONTEXT_ prefix (override system vars on collision)
    user_vars: dict[str, str] = {}
    if task.env:
        for k, v in task.env.items():
            user_vars[f"CLAW_CONTEXT_{k}"] = str(v)

    return {**os.environ, **system_vars, **user_vars}


def _write_context_file(task_name: str, context: dict) -> Path:
    """Write context JSON to fixed path file and return the path.

    Args:
        task_name: Task name used in filename.
        context: Context dict to write.

    Returns:
        Path to the written context file.
    """
    path = CONTEXT_INPUT_DIR / f"{task_name}_input.json"
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(json.dumps(context, ensure_ascii=False, indent=2))
    return path


def _parse_stdout_json(stdout: str) -> dict | None:
    """Parse the last line of stdout as JSON.

    Args:
        stdout: Full stdout string from subprocess.

    Returns:
        Parsed dict if last line is valid JSON, None otherwise.
    """
    if not stdout:
        return None
    last_line = stdout.rstrip("\n").rsplit("\n", 1)[-1].strip()
    try:
        parsed = json.loads(last_line)
        if isinstance(parsed, dict):
            return parsed
    except (json.JSONDecodeError, ValueError):
        pass
    return None
```
</action>

<acceptance_criteria>
- `executor.py` 包含 `import json` 和 `import os`
- `executor.py` 包含 `from claw_cron.context import load_context, save_context`
- `executor.py` 包含 `def _build_env(task: Task, context: dict)` 函数
- `executor.py` 包含 `def _write_context_file(task_name: str, context: dict)` 函数
- `executor.py` 包含 `def _parse_stdout_json(stdout: str)` 函数
- `executor.py` 包含 `CLAW_TASK_NAME` 字符串（系统变量定义）
- `executor.py` 包含 `CLAW_CONTEXT_FILE` 字符串（在 _write_context_file 或 execute_task 中）
</acceptance_criteria>

---

### Task 2: Modify execute_task() to inject context and parse stdout

<read_first>
- `src/claw_cron/executor.py` — `execute_task()` 函数（line 37-90），特别是 `subprocess.run` 调用
- `src/claw_cron/template.py` — `render()` 函数签名（Plan 19-01 创建）
</read_first>

<action>
修改 `executor.py`：

1. 在现有 imports 中添加：
   ```python
   from claw_cron.template import render as render_template
   ```

2. 修改 `execute_task()` 函数，在 `if task.type == "command":` 分支中，将：
   ```python
   cmd = task.script or ""
   ```
   替换为：
   ```python
   # Load context and build env
   context = load_context(task.name)
   env = _build_env(task, context)
   ctx_file = _write_context_file(task.name, context)
   env["CLAW_CONTEXT_FILE"] = str(ctx_file)

   # Render template variables in script
   raw_script = task.script or ""
   cmd = render_template(raw_script, context=context)
   ```

3. 修改 `subprocess.run` 调用，添加 `env` 参数（仅对 command 类型）：

   当前代码：
   ```python
   result = subprocess.run(cmd, shell=True, capture_output=True, text=True)
   ```

   由于 command 和 agent 类型共用同一个 `subprocess.run`，需要将 command 类型的执行分离。在 `_write_log(log_path, f"[{ts_start}] START: {task.name}\n")` 之后，将 `result = subprocess.run(...)` 替换为：

   ```python
   if task.type == "command":
       result = subprocess.run(cmd, shell=True, capture_output=True, text=True, env=env)
   else:
       result = subprocess.run(cmd, shell=True, capture_output=True, text=True)
   ```

4. 修改 `execute_task()` 返回值部分，在 `return result.returncode, output` 之前添加 stdout JSON 解析并将结果附加到返回值：

   将函数签名改为返回 `tuple[int, str, dict | None]`：
   ```python
   def execute_task(task: Task) -> tuple[int, str, dict | None]:
   ```

   在 `return result.returncode, output` 之前（command 类型分支内）：
   ```python
   # Parse stdout JSON feedback (command type only)
   feedback: dict | None = None
   if task.type == "command":
       feedback = _parse_stdout_json(result.stdout or "")
   return result.returncode, output, feedback
   ```

   对于 reminder 类型的 return，改为：
   ```python
   return 0, message, None
   ```

   对于 agent 类型（非 command），在 `return result.returncode, output` 改为：
   ```python
   return result.returncode, output, None
   ```

   **注意：** 由于 command 和 agent 共用同一个 `subprocess.run` 和 return，实际上只需在最终 return 前判断：
   ```python
   feedback = _parse_stdout_json(result.stdout or "") if task.type == "command" else None
   return result.returncode, output, feedback
   ```
</action>

<acceptance_criteria>
- `executor.py` 中 `execute_task` 函数签名包含 `tuple[int, str, dict | None]`
- `executor.py` 中 `subprocess.run` 调用（command 类型）包含 `env=env` 参数
- `executor.py` 包含 `_parse_stdout_json` 调用
- `executor.py` 包含 `load_context(task.name)` 调用
- `executor.py` 包含 `_write_context_file` 调用
- `executor.py` 包含 `render_template(raw_script, context=context)` 调用
- `python -c "from claw_cron.executor import execute_task; print('OK')"` 执行成功
</acceptance_criteria>

---

### Task 3: Update execute_task_with_notify() to save context feedback

<read_first>
- `src/claw_cron/executor.py` — `execute_task_with_notify()` 函数（line 93-140），需要处理新的三元组返回值
</read_first>

<action>
修改 `execute_task_with_notify()` 函数：

1. 将 `exit_code, output = execute_task(task)` 改为：
   ```python
   exit_code, output, feedback = execute_task(task)
   ```

2. 在 `if task.notify:` 块之前，添加上下文保存逻辑：
   ```python
   # Save feedback context if script returned JSON
   if feedback is not None:
       existing = load_context(task.name)
       merged = {**existing, **feedback}
       # Update system tracking fields
       merged["last_exit_code"] = exit_code
       merged["last_output"] = output[:4096] if output else ""
       merged["last_execution_time"] = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
       merged["execution_count"] = existing.get("execution_count", 0) + 1
       save_context(task.name, merged)
   else:
       # Still update system tracking fields even without feedback
       existing = load_context(task.name)
       existing["last_exit_code"] = exit_code
       existing["last_output"] = output[:4096] if output else ""
       existing["last_execution_time"] = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
       existing["execution_count"] = existing.get("execution_count", 0) + 1
       save_context(task.name, existing)
   ```
</action>

<acceptance_criteria>
- `executor.py` 中 `execute_task_with_notify` 包含 `exit_code, output, feedback = execute_task(task)`
- `executor.py` 包含 `save_context(task.name, merged)` 调用
- `executor.py` 包含 `execution_count` 字符串（计数更新）
- `executor.py` 包含 `last_execution_time` 字符串（时间戳更新）
- `python -c "from claw_cron.executor import execute_task_with_notify; print('OK')"` 执行成功
</acceptance_criteria>

## Verification

```bash
cd /Users/wxnacy/Projects/claw-cron
python -c "
from claw_cron.executor import execute_task
from claw_cron.storage import Task

# Test command task with JSON stdout feedback
task = Task(name='test_ctx', cron='* * * * *', type='command',
            script='echo hello && echo \\'{\\"signed_in\\": true}\\'')
exit_code, output, feedback = execute_task(task)
assert exit_code == 0, f'exit_code={exit_code}'
assert feedback == {'signed_in': True}, f'feedback={feedback}'
print(f'execute_task: OK, feedback={feedback}')
"
```

```bash
cd /Users/wxnacy/Projects/claw-cron
python -c "
from claw_cron.executor import execute_task
from claw_cron.storage import Task

# Test CLAW_ env vars are injected
task = Task(name='test_env', cron='* * * * *', type='command',
            script='echo \$CLAW_TASK_NAME')
exit_code, output, feedback = execute_task(task)
assert 'test_env' in output, f'CLAW_TASK_NAME not in output: {output}'
print('CLAW_TASK_NAME injection: OK')
"
```

## must_haves

- command 任务执行时 CLAW_TASK_NAME 等 8 个系统变量注入到子进程
- CLAW_CONTEXT_FILE 指向写入了上下文 JSON 的固定路径文件
- stdout 最后一行为有效 JSON 时，解析结果合并保存到 context.json
- execute_task 返回三元组 (exit_code, output, feedback)
