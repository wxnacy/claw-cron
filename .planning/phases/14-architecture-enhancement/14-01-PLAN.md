---
phase: 14
plan: 01
name: capture-abstraction
wave: 1
depends_on: []
files_modified:
  - src/claw_cron/channels/base.py
  - src/claw_cron/channels/qqbot.py
  - src/claw_cron/channels/feishu.py
  - src/claw_cron/cmd/channels.py
autonomous: true
requirements_addressed:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04
---

# Plan 14-01: Capture Abstraction Layer

## Objective

在 `MessageChannel` 基类中建立统一的 capture 抽象，将 `cmd/channels.py` 中的 capture 逻辑下沉到各通道类，并重构 `capture` 命令调用新接口。

## Tasks

### Task 1: 扩展 MessageChannel 基类

<read_first>
- `src/claw_cron/channels/base.py` — 当前基类结构，`send_template` 的 NotImplementedError 模式
- `src/claw_cron/channels/exceptions.py` — ChannelError 签名
</read_first>

<action>
在 `src/claw_cron/channels/base.py` 的 `MessageChannel` 类中新增两个方法，紧接在 `health_check()` 之后：

```python
@property
def supports_capture(self) -> bool:
    """Whether this channel supports openid capture via WebSocket."""
    return False

async def capture_openid(self, timeout: int = 300) -> str:
    """Capture user openid by waiting for an incoming message.

    Args:
        timeout: Timeout in seconds (default: 300s / 5 min).

    Returns:
        Captured openid string.

    Raises:
        NotImplementedError: If channel doesn't support capture.
        ChannelConfigError: If channel is not configured.
        ChannelError: If capture fails or times out.
    """
    raise NotImplementedError(
        f"{self.channel_id} does not support capture"
    )
```

同时在文件顶部 import 中确认 `ChannelError` 不需要导入（基类不直接使用，子类使用）。
</action>

<acceptance_criteria>
- `src/claw_cron/channels/base.py` 包含 `def supports_capture(self) -> bool:`
- `src/claw_cron/channels/base.py` 包含 `async def capture_openid(self, timeout: int = 300) -> str:`
- `src/claw_cron/channels/base.py` 包含 `raise NotImplementedError(`
- `grep -n "supports_capture" src/claw_cron/channels/base.py` 返回至少 2 行（property 装饰器 + def 行）
</acceptance_criteria>

---

### Task 2: QQBotChannel 实现 capture_openid()

<read_first>
- `src/claw_cron/channels/qqbot.py` — 当前类结构，`_validate_config()`、`_get_access_token()` 方法
- `src/claw_cron/qqbot/websocket.py` — `QQBotWebSocket`、`GatewayConfig` 的完整接口
- `src/claw_cron/cmd/channels.py` 第 461-535 行 — 现有 `_capture_qqbot_openid()` 逻辑（迁移来源）
- `src/claw_cron/channels/exceptions.py` — `ChannelError`、`ChannelConfigError`
</read_first>

<action>
在 `src/claw_cron/channels/qqbot.py` 中：

1. 在文件顶部 import 区新增：
```python
import asyncio
from claw_cron.qqbot import GatewayConfig, QQBotWebSocket
from .exceptions import ChannelError
```

2. 在 `QQBotChannel` 类中，`close()` 方法之前新增：

```python
@property
def supports_capture(self) -> bool:
    return True

async def capture_openid(self, timeout: int = 300) -> str:
    """Capture user openid via QQ Bot WebSocket.

    Connects to QQ Bot Gateway and waits for the user to send any message.
    Returns the sender's openid.

    Args:
        timeout: Timeout in seconds (default: 300).

    Returns:
        Captured openid string (bot-specific, use as c2c:OPENID recipient).

    Raises:
        ChannelConfigError: If app_id or client_secret is not configured.
        ChannelError: If capture times out or connection fails.
    """
    token = await self._get_access_token()  # raises ChannelConfigError if not configured

    gateway_config = GatewayConfig(app_id=self.config.app_id, access_token=token)
    ws_client = QQBotWebSocket(gateway_config)
    captured_openid: str | None = None

    async def on_message(message) -> None:
        nonlocal captured_openid
        captured_openid = message.openid
        await ws_client.close()

    ws_client.on_c2c_message = on_message

    async def _do_capture() -> str:
        await asyncio.gather(ws_client.connect(), _wait_for_capture())
        return captured_openid  # type: ignore[return-value]

    async def _wait_for_capture() -> None:
        while not captured_openid:
            await asyncio.sleep(0.1)

    try:
        return await asyncio.wait_for(_do_capture(), timeout=timeout)
    except asyncio.TimeoutError:
        await ws_client.close()
        raise ChannelError(
            f"Capture timed out after {timeout}s", channel_id=self.channel_id
        )
```
</action>

<acceptance_criteria>
- `grep -n "supports_capture" src/claw_cron/channels/qqbot.py` 返回结果（property 定义）
- `grep -n "async def capture_openid" src/claw_cron/channels/qqbot.py` 返回结果
- `grep -n "asyncio.wait_for" src/claw_cron/channels/qqbot.py` 返回结果（超时机制）
- `grep -n "ChannelError" src/claw_cron/channels/qqbot.py` 返回至少 2 行（import + raise）
- `grep -n "GatewayConfig\|QQBotWebSocket" src/claw_cron/channels/qqbot.py` 返回至少 2 行
</acceptance_criteria>

---

### Task 3: FeishuChannel 实现 capture_openid()

<read_first>
- `src/claw_cron/channels/feishu.py` — 当前类结构，`_validate_config()` 方法
- `src/claw_cron/feishu/events.py` — `parse_feishu_message()` 函数签名
- `src/claw_cron/cmd/channels.py` 第 538-616 行 — 现有 `_capture_feishu_openid()` 逻辑（迁移来源）
- `src/claw_cron/channels/exceptions.py` — `ChannelError`
</read_first>

<action>
在 `src/claw_cron/channels/feishu.py` 中：

1. 在文件顶部 import 区新增：
```python
import asyncio
from .exceptions import ChannelError
```

2. 在 `FeishuChannel` 类中，`close()` 方法之前新增：

```python
@property
def supports_capture(self) -> bool:
    return True

async def capture_openid(self, timeout: int = 300) -> str:
    """Capture user openid via Feishu WebSocket.

    Connects to Feishu WebSocket and waits for the user to send any message.
    Returns the sender's open_id.

    Args:
        timeout: Timeout in seconds (default: 300).

    Returns:
        Captured open_id string (starts with "ou_").

    Raises:
        ChannelConfigError: If app_id or app_secret is not configured.
        ChannelError: If capture times out or connection fails.
    """
    self._validate_config()

    import lark_oapi as lark
    from claw_cron.feishu.events import parse_feishu_message

    captured_openid: str | None = None

    def on_message_received(data: lark.im.v1.P2ImMessageReceiveV1) -> None:
        nonlocal captured_openid
        event_data = {
            "sender": {"sender_id": {"open_id": data.event.sender.sender_id.open_id}},
            "message": {
                "content": data.event.message.content,
                "message_id": data.event.message.message_id,
                "chat_id": data.event.message.chat_id,
                "create_time": data.event.message.create_time,
            },
        }
        message = parse_feishu_message(event_data)
        captured_openid = message.openid

    event_handler = (
        lark.EventDispatcherHandler.builder("", "")
        .register_p2_im_message_receive_v1(on_message_received)
        .build()
    )
    ws_client = lark.ws.Client(
        self.config.app_id,
        self.config.app_secret,
        event_handler=event_handler,
        log_level=lark.LogLevel.ERROR,
    )

    async def _wait_for_capture() -> None:
        while not captured_openid:
            await asyncio.sleep(0.1)

    async def _do_capture() -> str:
        await asyncio.gather(ws_client.start(), _wait_for_capture())
        return captured_openid  # type: ignore[return-value]

    try:
        return await asyncio.wait_for(_do_capture(), timeout=timeout)
    except asyncio.TimeoutError:
        raise ChannelError(
            f"Capture timed out after {timeout}s", channel_id=self.channel_id
        )
```
</action>

<acceptance_criteria>
- `grep -n "supports_capture" src/claw_cron/channels/feishu.py` 返回结果
- `grep -n "async def capture_openid" src/claw_cron/channels/feishu.py` 返回结果
- `grep -n "asyncio.wait_for" src/claw_cron/channels/feishu.py` 返回结果
- `grep -n "ChannelError" src/claw_cron/channels/feishu.py` 返回至少 2 行（import + raise）
- `grep -n "parse_feishu_message" src/claw_cron/channels/feishu.py` 返回结果
</acceptance_criteria>

---

### Task 4: 重构 cmd/channels.py 的 capture 命令

<read_first>
- `src/claw_cron/cmd/channels.py` — 完整文件，重点是 `capture()` 命令（第 440-460 行）和 `_capture_qqbot_openid()`、`_capture_feishu_openid()` 函数（第 461-616 行）
- `src/claw_cron/channels/__init__.py` — `CHANNEL_REGISTRY` 结构，`get_channel()` 工厂函数
- `src/claw_cron/channels/exceptions.py` — `ChannelError`、`ChannelConfigError`
</read_first>

<action>
在 `src/claw_cron/cmd/channels.py` 中：

1. 在文件顶部 import 区新增：
```python
from claw_cron.channels import get_channel
from claw_cron.channels.exceptions import ChannelError, ChannelConfigError
```

2. 将 `capture` 命令替换为：
```python
@channels.command()
@click.option(
    "--channel-type",
    type=click.Choice(["qqbot", "feishu"], case_sensitive=False),
    default="qqbot",
    help="Channel to capture openid from",
)
@click.option(
    "--alias",
    prompt="Save as contact alias",
    default="me",
    help="Alias name for the captured contact",
)
def capture(channel_type: str, alias: str) -> None:
    """Connect to channel and capture user openid."""
    channel = get_channel(channel_type)

    if not channel.supports_capture:
        console.print(f"[yellow]通道 '{channel_type}' 不需要 capture[/yellow]")
        return

    console.print(f"\n[bold]等待消息...[/bold]")
    console.print(f"[dim]向你的 {channel_type} 机器人发送任意消息以捕获 openid[/dim]")
    console.print("[dim]按 Ctrl+C 取消[/dim]\n")

    try:
        with console.status(f"[bold green]等待来自 {channel_type} 的消息..."):
            openid = asyncio.run(channel.capture_openid())
        console.print(f"[green]✓ OpenID 已捕获: [bold]{openid}[/bold][/green]")

        contact = Contact(
            openid=openid,
            channel=channel_type,
            alias=alias,
            created=datetime.now().isoformat(),
        )
        save_contact(contact)
        console.print(f"[green]✓ 联系人已保存为 '[bold]{alias}[/bold]'[/green]")
        console.print(f"[dim]现在可以使用 'claw-cron remind --recipient {alias}'[/dim]")
    except KeyboardInterrupt:
        console.print("\n[yellow]已取消[/yellow]")
    except (ChannelError, ChannelConfigError) as e:
        console.print(f"[red]Capture 失败: {e}[/red]")
        raise SystemExit(1)
```

3. 删除 `_capture_qqbot_openid()` 和 `_capture_feishu_openid()` 两个函数（第 461-616 行）。

4. 删除不再需要的 import（如果 `GatewayConfig`、`QQBotWebSocket`、`QQBotConfig` 仅在这两个函数中使用）：
   - 检查并移除 `from claw_cron.qqbot import GatewayConfig, QQBotWebSocket`
   - 检查并移除 `from claw_cron.channels.qqbot import QQBotConfig`
</action>

<acceptance_criteria>
- `grep -n "_capture_qqbot_openid\|_capture_feishu_openid" src/claw_cron/cmd/channels.py` 返回空（函数已删除）
- `grep -n "channel.capture_openid\|channel\.supports_capture" src/claw_cron/cmd/channels.py` 返回结果
- `grep -n "from claw_cron.channels import get_channel" src/claw_cron/cmd/channels.py` 返回结果
- `grep -n "ChannelError\|ChannelConfigError" src/claw_cron/cmd/channels.py` 返回至少 2 行（import + except）
- `python -c "from claw_cron.cmd.channels import channels"` 执行无报错
</acceptance_criteria>

---

## Verification

```bash
# 1. 语法检查
python -c "from claw_cron.channels.base import MessageChannel; print('base OK')"
python -c "from claw_cron.channels.qqbot import QQBotChannel; ch = QQBotChannel(); print('supports_capture:', ch.supports_capture)"
python -c "from claw_cron.channels.feishu import FeishuChannel; ch = FeishuChannel(); print('supports_capture:', ch.supports_capture)"
python -c "from claw_cron.channels.imessage import IMessageChannel; ch = IMessageChannel(); print('supports_capture:', ch.supports_capture)"
python -c "from claw_cron.cmd.channels import channels; print('cmd OK')"

# 2. 验证 supports_capture 默认值
python -c "
from claw_cron.channels import CHANNEL_REGISTRY
for ch_id, ch_class in CHANNEL_REGISTRY.items():
    ch = ch_class()
    print(f'{ch_id}: supports_capture={ch.supports_capture}')
"
# 期望输出：
# email: supports_capture=False
# feishu: supports_capture=True
# imessage: supports_capture=False
# qqbot: supports_capture=True

# 3. 验证 capture_openid 基类抛出 NotImplementedError
python -c "
import asyncio
from claw_cron.channels.imessage import IMessageChannel
ch = IMessageChannel()
try:
    asyncio.run(ch.capture_openid())
except NotImplementedError as e:
    print(f'NotImplementedError OK: {e}')
"
```

## must_haves

- [ ] `MessageChannel.supports_capture` 属性存在，默认返回 `False`
- [ ] `MessageChannel.capture_openid()` 方法存在，默认抛出 `NotImplementedError`
- [ ] `QQBotChannel.supports_capture` 返回 `True`
- [ ] `FeishuChannel.supports_capture` 返回 `True`
- [ ] `QQBotChannel.capture_openid()` 包含 `asyncio.wait_for` 超时机制
- [ ] `FeishuChannel.capture_openid()` 包含 `asyncio.wait_for` 超时机制
- [ ] `cmd/channels.py` 中 `_capture_qqbot_openid` 和 `_capture_feishu_openid` 已删除
- [ ] `cmd/channels.py` 中 `capture` 命令调用 `channel.capture_openid()`
- [ ] 所有模块可正常导入（无 ImportError）
