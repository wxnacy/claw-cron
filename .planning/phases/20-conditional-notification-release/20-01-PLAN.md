---
plan: 20-01
phase: 20
title: Conditional Notification & Release
wave: 1
depends_on: []
files_modified:
  - src/claw_cron/condition.py
  - src/claw_cron/executor.py
  - src/claw_cron/__about__.py
autonomous: true
requirements_addressed:
  - COND-02
  - COND-03
  - VER-01
---

# Plan 20-01: Conditional Notification & Release

## Objective

实现 when 条件表达式求值器，在 executor.py 通知前评估条件，确保向后兼容，并升级版本号到 0.3.0。

## Tasks

### Task 1: 创建 condition.py — when 表达式求值模块

<read_first>
- `src/claw_cron/template.py` — 参考正则实现风格
- `src/claw_cron/notifier.py` — NotifyConfig.when 字段定义
</read_first>

<action>
新建 `src/claw_cron/condition.py`，实现以下内容：

```python
# SPDX-FileCopyrightText: 2026-present wxnacy <371032668@qq.com>
#
# SPDX-License-Identifier: MIT

"""When condition expression evaluator for conditional notifications."""

from __future__ import annotations

import logging
import re

logger = logging.getLogger(__name__)

# Pattern: key (==|!=) value
_EXPR_RE = re.compile(r"^\s*(\w+)\s*(==|!=)\s*(.+?)\s*$")


def _coerce(value: str) -> bool | str | int | float:
    """Coerce string literal to Python type.

    - "true" / "false" -> bool
    - integer string -> int
    - float string -> float
    - otherwise -> str (strip surrounding quotes)
    """
    if value.lower() == "true":
        return True
    if value.lower() == "false":
        return False
    try:
        return int(value)
    except ValueError:
        pass
    try:
        return float(value)
    except ValueError:
        pass
    # Strip surrounding single or double quotes
    if (value.startswith('"') and value.endswith('"')) or (
        value.startswith("'") and value.endswith("'")
    ):
        return value[1:-1]
    return value


def evaluate_when(when: str | None, context: dict) -> bool:
    """Evaluate a when expression against a context dict.

    Supports == and != operators only.
    On parse error or missing key, logs a warning and returns True
    (conservative: send notification rather than silently suppress).

    Args:
        when: Expression string like 'signed_in == false' or None.
        context: Task context dict (merged, from executor).

    Returns:
        True if notification should be sent, False if suppressed.
    """
    if not when:
        return True

    m = _EXPR_RE.match(when)
    if not m:
        logger.warning(
            "when expression %r could not be parsed — sending notification", when
        )
        return True

    key, op, raw_value = m.group(1), m.group(2), m.group(3)

    if key not in context:
        logger.warning(
            "when expression %r: key %r not found in context — sending notification",
            when,
            key,
        )
        return True

    ctx_val = context[key]
    expr_val = _coerce(raw_value)

    try:
        if op == "==":
            result = ctx_val == expr_val
        else:  # !=
            result = ctx_val != expr_val
    except Exception:
        logger.warning(
            "when expression %r: comparison failed — sending notification", when
        )
        return True

    if not result:
        logger.info("Notification suppressed: when condition %r not met", when)
    return result
```
</action>

<acceptance_criteria>
- `src/claw_cron/condition.py` 文件存在
- 文件包含 `def evaluate_when(` 函数定义
- 文件包含 `_EXPR_RE = re.compile(` 正则定义
- 文件包含 `def _coerce(` 辅助函数
- `evaluate_when(None, {})` 返回 True（无 when 时始终发送）
- `evaluate_when("signed_in == false", {"signed_in": False})` 返回 True（条件满足）
- `evaluate_when("signed_in == false", {"signed_in": True})` 返回 False（条件不满足，抑制通知）
- `evaluate_when("status != ok", {"status": "ok"})` 返回 False
- `evaluate_when("missing_key == true", {})` 返回 True（缺失键保守策略）
- `evaluate_when("bad expression", {})` 返回 True（解析失败保守策略）
</acceptance_criteria>

---

### Task 2: 修改 executor.py — 在通知前插入 when 条件检查

<read_first>
- `src/claw_cron/executor.py` — 完整文件，重点关注 `execute_task_with_notify()` 函数（line 181），`if task.notify:` 块（line 205）
- `src/claw_cron/condition.py` — 刚创建的求值模块
</read_first>

<action>
在 `executor.py` 中做以下修改：

1. 在文件顶部 import 区域添加：
```python
from claw_cron.condition import evaluate_when
```

2. 在 `execute_task_with_notify()` 函数的 `if task.notify:` 块内，`notifier = Notifier()` 之前插入 when 条件检查：

将现有代码：
```python
    if task.notify:
        try:
            notifier = Notifier()
```

替换为：
```python
    if task.notify and evaluate_when(task.notify.when, merged):
        try:
            notifier = Notifier()
```

注意：`merged` 变量在此时已经包含了 feedback 合并结果和系统字段（last_exit_code 等），直接使用即可。
</action>

<acceptance_criteria>
- `src/claw_cron/executor.py` 包含 `from claw_cron.condition import evaluate_when`
- `executor.py` 中 `if task.notify and evaluate_when(task.notify.when, merged):` 这行存在
- 原有 `if task.notify:` 不再单独出现（已被替换为带条件的版本）
- `execute_task_with_notify` 函数中 `evaluate_when` 调用传入 `merged` 作为 context 参数
</acceptance_criteria>

---

### Task 3: 更新版本号到 0.3.0

<read_first>
- `src/claw_cron/__about__.py` — 当前版本 `0.2.1`
</read_first>

<action>
将 `src/claw_cron/__about__.py` 中的版本号从 `0.2.1` 更新为 `0.3.0`：

```python
__version__ = "0.3.0"
```
</action>

<acceptance_criteria>
- `src/claw_cron/__about__.py` 包含 `__version__ = "0.3.0"`
- 文件不再包含 `0.2.1`
</acceptance_criteria>

---

## Verification

```bash
# 1. 确认新文件存在
test -f src/claw_cron/condition.py && echo "condition.py OK"

# 2. 确认 evaluate_when 函数定义
grep -n "def evaluate_when" src/claw_cron/condition.py

# 3. 确认 executor.py 导入和条件检查
grep -n "from claw_cron.condition import evaluate_when" src/claw_cron/executor.py
grep -n "evaluate_when" src/claw_cron/executor.py

# 4. 确认版本号
grep "__version__" src/claw_cron/__about__.py

# 5. 语法检查
python -c "from claw_cron.condition import evaluate_when; print('import OK')"
python -c "from claw_cron.executor import execute_task_with_notify; print('import OK')"

# 6. 快速功能验证
python -c "
from claw_cron.condition import evaluate_when
assert evaluate_when(None, {}) == True
assert evaluate_when('signed_in == false', {'signed_in': False}) == True
assert evaluate_when('signed_in == false', {'signed_in': True}) == False
assert evaluate_when('status != ok', {'status': 'ok'}) == False
assert evaluate_when('missing == true', {}) == True
assert evaluate_when('bad expr!!', {}) == True
print('All assertions passed')
"
```

## must_haves

- [ ] `condition.py` 存在且 `evaluate_when` 可正常导入
- [ ] when 为 None 时通知正常发送（向后兼容，COND-03）
- [ ] when 条件满足时通知发送，不满足时抑制（COND-02）
- [ ] 求值失败时保守策略：发送通知而非静默丢弃（D-08）
- [ ] 版本号更新为 0.3.0（VER-01）
