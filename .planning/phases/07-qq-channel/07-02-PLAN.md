---
phase: 07-qq-channel
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/claw_cron/channels/qqbot.py
autonomous: true
requirements:
  - QQ-01
  - QQ-04
  - QQ-05
  - QQ-06
must_haves:
  truths:
    - "User can send private chat message using c2c:OPENID format"
    - "User can send group chat message using group:GROUP_OPENID format"
    - "User can send markdown formatted messages"
    - "Rate limit errors are retried automatically with exponential backoff"
    - "Auth errors are reported clearly without retrying"
  artifacts:
    - path: "src/claw_cron/channels/qqbot.py"
      provides: "Complete QQ Bot channel implementation"
      min_lines: 250
      exports: ["QQBotChannel", "QQBotConfig", "RecipientType", "RecipientInfo"]
  key_links:
    - from: "QQBotChannel.send_text"
      to: "/v2/users/{openid}/messages"
      via: "HTTP POST with Authorization header"
      pattern: "v2/users/.*/messages"
    - from: "QQBotChannel.send_text"
      to: "/v2/groups/{openid}/messages"
      via: "HTTP POST for group messages"
      pattern: "v2/groups/.*/messages"
    - from: "QQBotChannel._send_with_retry"
      to: "tenacity.retry"
      via: "decorator"
      pattern: "@retry"
user_setup: []
---

# Phase 7 Plan 02: QQ Bot Message Sending

<objective>
实现 QQ Bot 的消息发送功能，支持私聊、群聊和 Markdown 格式。

Purpose: 使 QQBotChannel 能够实际发送消息，满足定时任务通知需求。
Output: 完整的 QQBotChannel 实现，支持 C2C 和 Group 消息发送。
</objective>

<execution_context>
@${CODEBUDDY_PLUGIN_ROOT}/workflows/execute-plan.md
@${CODEBUDDY_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-qq-channel/07-RESEARCH.md
@.planning/phases/07-qq-channel/07-01-SUMMARY.md
@src/claw_cron/channels/base.py
@src/claw_cron/channels/exceptions.py
</context>

<interfaces>

From src/claw_cron/channels/base.py:
```python
@dataclass
class MessageResult:
    success: bool
    message_id: str | None = None
    error: str | None = None
    timestamp: int | None = None
    raw_response: Any = None
```

From 07-01 implementation (expected):
```python
class QQBotChannel(MessageChannel):
    config: QQBotConfig
    _token: TokenInfo | None
    _http_client: httpx.AsyncClient

    async def _get_access_token(self) -> str: ...
```

From 07-RESEARCH.md Pattern 3:
```python
class RecipientType(Enum):
    C2C = "c2c"
    GROUP = "group"

@dataclass
class RecipientInfo:
    type: RecipientType
    openid: str

def parse_recipient(recipient: str) -> RecipientInfo:
    # "c2c:OPENID" -> RecipientInfo(C2C, openid)
    # "group:GROUP_OPENID" -> RecipientInfo(GROUP, openid)
    # Plain string -> RecipientInfo(C2C, string)
```

From 07-RESEARCH.md Code Examples:
```python
# C2C: POST /v2/users/{user_openid}/messages
# Group: POST /v2/groups/{group_openid}/messages
# Body: {"content": str, "msg_type": int}
# Markdown: {"msg_type": 2, "markdown": {"content": str}}
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement recipient parsing and C2C message sending</name>
  <files>src/claw_cron/channels/qqbot.py</files>
  <behavior>
    - Test 1: parse_recipient("c2c:ABC123") returns RecipientInfo(C2C, "ABC123")
    - Test 2: parse_recipient("group:XYZ789") returns RecipientInfo(GROUP, "XYZ789")
    - Test 3: parse_recipient("PLAIN") returns RecipientInfo(C2C, "PLAIN")
    - Test 4: send_text with c2c:OPENID sends to /v2/users/{openid}/messages
    - Test 5: send_text returns MessageResult with success=True on 200 response
  </behavior>
  <action>
1. Add RecipientType enum and RecipientInfo dataclass:
   - Follow RESEARCH.md Pattern 3 exactly
   - Add parse_recipient() function with format validation
   - Support formats: "c2c:OPENID", "group:GROUP_OPENID", plain string (treat as C2C)

2. Implement _send_with_retry() private async method:
   - Use tenacity @retry decorator with exponential backoff
   - Retry on: HTTP 429, HTTP 500+, error codes 22009, 20028, 304045-304050
   - Do NOT retry on auth errors (11241-11261)
   - Get token via _get_access_token()
   - POST to endpoint with Authorization: QQBot {token}
   - Parse response and handle error codes
   - Return response dict

3. Implement send_text() method:
   - Parse recipient with parse_recipient()
   - Determine endpoint based on RecipientType:
     - C2C: /v2/users/{openid}/messages
     - GROUP: /v2/groups/{openid}/messages
   - Build payload: {"content": content, "msg_type": 0}
   - Call _send_with_retry()
   - Return MessageResult with success, message_id, timestamp from response

Per QQ-04 requirement: 支持私聊消息发送 (c2c:OPENID 格式)

Reference: RESEARCH.md Pattern 3 (Recipient Format Parsing), Code Example: C2C Private Chat Message
  </action>
  <verify>
    <automated>python -c "from claw_cron.channels.qqbot import parse_recipient, RecipientType; r = parse_recipient('c2c:ABC123'); assert r.type == RecipientType.C2C and r.openid == 'ABC123'"</automated>
  </verify>
  <done>
- RecipientType enum and RecipientInfo dataclass
- parse_recipient() function with format validation
- _send_with_retry() with tenacity retry logic
- send_text() implementation for C2C messages
- MessageResult returned with proper success/error handling
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement group message sending</name>
  <files>src/claw_cron/channels/qqbot.py</files>
  <behavior>
    - Test 1: send_text with group:OPENID sends to /v2/groups/{openid}/messages
    - Test 2: Group message payload is same format as C2C
    - Test 3: Group message returns MessageResult with message_id
  </behavior>
  <action>
1. Verify send_text() supports both C2C and GROUP:
   - Already implemented in Task 1 with endpoint selection
   - Test both paths manually with Python

2. Add group-specific error handling:
   - Group rate limit: 4 messages per group per month (active)
   - Error code 304045-304050 are group-specific rate limits
   - Add to retry list if not already included

3. Add docstring examples:
   - Show c2c:OPENID format example
   - Show group:GROUP_OPENID format example
   - Explain how to get openid from bot events

Per QQ-05 requirement: 支持群聊消息发送 (group:GROUP_OPENID 格式)

Reference: RESEARCH.md Code Example: Group Chat Message
  </action>
  <verify>
    <automated>python -c "from claw_cron.channels.qqbot import parse_recipient, RecipientType; r = parse_recipient('group:XYZ789'); assert r.type == RecipientType.GROUP and r.openid == 'XYZ789'"</automated>
  </verify>
  <done>
- send_text() supports both C2C and GROUP recipient types
- Proper endpoint selection based on recipient type
- Group-specific rate limit errors are retried
- Docstrings include examples for both formats
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Markdown message support</name>
  <files>src/claw_cron/channels/qqbot.py</files>
  <behavior>
    - Test 1: send_markdown sends with msg_type=2
    - Test 2: Markdown payload uses {"msg_type": 2, "markdown": {"content": ...}}
    - Test 3: send_markdown returns MessageResult on success
    - Test 4: Plain text fallback for channels not supporting markdown (already in base)
  </behavior>
  <action>
1. Implement send_markdown() method:
   - Parse recipient with parse_recipient()
   - Determine endpoint (same as send_text)
   - Build payload: {"msg_type": 2, "markdown": {"content": content}}
   - Call _send_with_retry()
   - Return MessageResult

2. Add Markdown limitations to docstring:
   - Note: msg_type=2 requires internal invitation in production
   - Sandbox environment allows free testing
   - If markdown fails, fallback to plain text (msg_type=0)
   - Reference Pitfall 3 from RESEARCH.md

3. Implement graceful fallback:
   - Catch error code 50056 ("不允许发送 markdown content")
   - Log warning about markdown limitation
   - Retry with msg_type=0 (plain text)
   - Add "markdown_fallback" flag in raw_response

Per QQ-06 requirement: 支持 Markdown 消息格式 (msg_type=2)

Reference: RESEARCH.md Code Example: Markdown Message, Pitfall 3 (Markdown Not Working in Production)
  </action>
  <verify>
    <automated>python -c "from claw_cron.channels.qqbot import QQBotChannel; import inspect; assert 'send_markdown' in dir(QQBotChannel)"</automated>
  </verify>
  <done>
- send_markdown() implemented with msg_type=2
- Markdown payload format: {"msg_type": 2, "markdown": {"content": ...}}
- Graceful fallback to plain text on error 50056
- Docstring documents markdown limitations
- Both C2C and GROUP support markdown messages
  </done>
</task>

</tasks>

<verification>
1. parse_recipient() correctly parses "c2c:OPENID" format
2. parse_recipient() correctly parses "group:GROUP_OPENID" format
3. parse_recipient() treats plain string as C2C
4. send_text() sends to /v2/users/{openid}/messages for C2C
5. send_text() sends to /v2/groups/{openid}/messages for GROUP
6. send_markdown() uses msg_type=2 with markdown payload
7. Rate limit errors (429, 22009, etc.) are retried automatically
8. Auth errors (11241-11261) are not retried
9. Markdown fallback to plain text on error 50056
</verification>

<success_criteria>
- [x] C2C messages work with "c2c:OPENID" format (QQ-04)
- [x] Group messages work with "group:GROUP_OPENID" format (QQ-05)
- [x] Markdown messages work with msg_type=2 (QQ-06)
- [x] Rate limit errors are retried with exponential backoff (QQ-01)
- [x] Auth errors are reported clearly without retry (QQ-01)
- [x] Markdown has graceful fallback to plain text (QQ-06)
</success_criteria>

<output>
After completion, create `.planning/phases/07-qq-channel/07-02-SUMMARY.md`
</output>
